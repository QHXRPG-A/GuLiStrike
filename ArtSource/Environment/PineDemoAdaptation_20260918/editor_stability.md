# 原地图补拍期间的编辑器退出记录

## 现象与冻结证据

源码版 UE5.7 在本次原地图适配期间退出两次。最近一次日志确认是崩溃，并非正常关闭；不再将其描述为用户关闭。原地图和资源此前已保存并重新打开，布局读回通过。

- 场景：`/Game/StylizedPineEnvironment/Maps/Demo_Map`，编辑器非 PIE。
- 预期：保持编辑器运行，读取材质/布局并拍摄原地图。
- 观察：2026-09-18 01:55:05（北京时间；日志 UTC 为 2026.09.17-17.55.05）渲染工作线程退出。
- 冻结的完整日志：[editor_crash_20260918_015505.log](editor_crash_20260918_015505.log)。这是退出后、再次启动之前从已知日志文件直接复制的证据，没有遍历 Saved。
- 关键日志：`ParallelDraw (Index: 0/1, Num: 2)` → `RenderVelocities(Opaque)` → `Runnable thread Foreground Worker #1/#0 crashed` → `RequestExitWithStatus(1, 3, FRunnableThreadWin::GuardedRun.ExceptionHandler)`。
- 崩溃报告客户端 `CrashReportClientEditor-Win64-Debug.exe` 未能启动；现有日志没有提供完整异常调用栈。日志显示进程物理内存约 5.7GB、系统空闲约 13.6GB，不能直接归因为系统物理内存耗尽。

## 定位边界与候选原因

最后可见失败域是速度渲染的并行绘制，但这只是观察点，不是已经确定的根因。资源编译成功、地图检查零错误和保存读回成功也不能证明运行稳定性通过。

按验证成本排序：

1. 本地插件的 `MaterialNodeService.get_material_diagnostics` 会调用 `ForceRecompileForRendering`，并非纯只读查询。两次退出前的工作中都使用过该接口；与崩溃的因果关系尚未建立。先停止重复调用，正常重新打开已保存地图补拍，不引入更多材质重编。
2. 并行速度渲染或渲染资源重建的引擎/插件时序问题。已只读核对引擎 `VelocityRendering.cpp` 的 `r.ParallelVelocity` 开关，但本次未更改它；不改引擎源码、不擅改项目配置。
3. 其他渲染、驱动或资源状态问题。缺少完整调用栈，不据此更换驱动、编译引擎或宣布修复。

## 当前处置与验证范围

2026-09-18 02:08:51 以默认渲染配置重新启动源码版编辑器，进程 11188，直接打开已保存 Demo_Map。仅继续本需求原有截图/保存读回范围；不追加测试套件或邻接模块测试。TAA和100%屏幕比例如用于预览，仍仅是会话设置。

本次结果：启动后405个当前Actor、空脏包；保存的天空配色与参数一致。02:11完成九机位补拍（含修正后的林下/符文），未再退出。随后离开并重新打开Demo_Map，不手动重捕天空，在项目原有FXAA下补拍入口，颜色保持。并行速度渲染仍为默认 `r.ParallelVelocity=1`，没有使用关闭并行绘制的绕过方案。最终布局/资源覆盖读回通过；材质重编诊断未再次执行。

这只说明本次观察窗口内顺利完成，不等同于找到了或修复了上述崩溃根因。当前不阻断交付已保存的地图和审核图片；引擎/插件内部稳定性需有完整异常栈或可复现条件后单独排查，未在本美术任务中擅自修改其源码。
