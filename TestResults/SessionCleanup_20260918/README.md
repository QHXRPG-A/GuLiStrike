# 本会话临时文件清理

用户要求删除本会话产生的临时文件。已清理能够明确归属本会话、且不承担最终交付或必要证据用途的598个文件，共595,814,484字节（约568.2 MiB）。所有文件逐项验证绝对路径、SHA256、大小、非链接目录和未被占用后移入Windows回收站；没有递归删除。

| 类别 | 数量 |
|---|---:|
| 已合成最终MP4的爆炸逐帧PNG | 495 |
| 未被报告/脚本引用的旧会话日志 | 82 |
| Python编译缓存 | 9 |
| 失败工具调用的JSON返回 | 7 |
| 未被最终报告引用的旧诊断尝试 | 5 |

[逐项路径、哈希和保留清单](plan.json)；[执行与复核结果](result.json)。

## 保留范围

- 正式UE资源、地图、代码、数据、Blender/FBX制作源文件、原始参考图及其他任务目录。
- 最终验收图、三个MP4、审核页面、迁移清单、原始通过报告，以及仍被记录引用的失败定位证据。
- 两张玩法地图的本地回退备份、松树林原包备份；本次没有将其当成已确认临时文件删除。
- Git历史与LFS对象。删除当前版本的文件不等于从GitHub历史中抹除它们。

三个最终MP4与两份审核页面清理前后SHA256相同。原PNG序列可按脚本重新捕获，也可从清理前提交恢复；运行报告的`capture.json`保留历史生成参数，其中记录的逐帧输出目录不再是当前保留交付物。

GitHub在清理前已完成同步：`0fb0d26ac9314bcae95a1a37f669926e35e7ffca`，本地与远端main核对一致。已经版本化的临时文件还可从该提交恢复；未入库的日志/缓存可从回收站恢复，缓存也可再生。不要为了取回个别文件重置整个工作树。

新增忽略规则防止爆炸逐帧PNG再次随最终视频入库。本记录与计划清单是清理审计资料，不属于待删除的中间输出。后续删除空目录的命令被执行策略在启动前拦截，因此空目录保留；这不影响已成功移除的598个文件。
