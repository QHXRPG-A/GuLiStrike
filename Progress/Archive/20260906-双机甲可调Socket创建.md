---
schema: guli-progress/v1
id: ARC-20260906-001
work_id: ''
kind: archive
role: root
title: 2026-09-06 解决了：为两兵种添加可供用户精调的 Socket
areas:
- commander
- data-pipeline
- vfx
- combat
- assets
categories:
- art
- gameplay
status: recorded
verification: partial
created: '2026-09-06'
updated: '2026-09-06'
summary: 通过源码 UE5.7.4、现有端口 12029 的 VibeUE SkeletonService.addsocket(..., addtoskeleton=False) 创建挂点。只保存两个明确修改的 SKM 资产，未改原骨、蒙皮、独立
  Skeleton 或 Control Rig 图
next_action: ''
relations:
  work_items:
  - WORK-20260906-002
status_note: ''
---

# 2026-09-06 解决了：为两兵种添加可供用户精调的 Socket

- 对应开发文档：[指挥官双机甲骨骼与武器挂点](../DevelopmentDocumentation/20260906-指挥官双机甲骨骼与武器挂点.md)
- 变更类型：内容资产、编辑器 Python、文档。
- 追加要求：用户要求“你帮我加 socket，我来调整，调好了告诉你”。本次只完成创建和初始摆放，不替用户完成出口校准或接入战斗。

## 变更清单

| 文件/资产 | 变更 |
|---|---|
| `/Game/GuLiStrike/Robots/FourFRobot/Rig/SKM_FourFRobot_Rig` | 新增 2 个 Mesh Socket |
| `/Game/GuLiStrike/Robots/WM01/Rig/SKM_WM01_Rig` | 新增 4 个 Mesh Socket，原 9 个挂点保留 |
| `Scripts/build_commander_rigs.py` | 添加 `add_adjustable_sockets()`；仅新增缺失项，绝不覆盖用户已调 Socket |
| 开发文档、README、指挥官资产状态说明 | 更新本轮实际状态 |

## 做了什么

通过源码 UE5.7.4、现有端口 12029 的 VibeUE `SkeletonService.add_socket(..., add_to_skeleton=False)` 创建挂点。只保存两个明确修改的 `SKM_` 资产，未改原骨、蒙皮、独立 Skeleton 或 Control Rig 图。

| 单位 | Socket | 父骨 | 初始参考网格位置，cm |
|---|---|---|---|
| FourFRobot | `FX_Muzzle_Basic_01` | `engranaje_ca��on3_019`（原 FName，未改名） | (-480, 5, 935) |
| FourFRobot | `FX_AimTarget` | `centro_00` | (0, 0, 650) |
| WM01 | `FX_Muzzle_Basic_01` | `Front_minigun` | (1490, 466, 1810) |
| WM01 | `FX_AimTarget` | `Mainbody` | (0, 495, 1600) |
| WM01 | `FX_Missile_01` | `Missile_launcher_01` | (-260, 1257, 2440) |
| WM01 | `FX_Missile_02` | `Missile_launcher_02` | (-260, -326, 2440) |

这些位置仅是沿用既有发射/瞄准配置的近似种子，不是已经校准的 Socket→Crowd 映射。写入 Socket 时已换算到父骨局部空间，不能直接用上表数字替换 Details 的 Relative Location。Socket 局部 Scale 为 1；FourF 保留原父骨绑定尺度。

枪口初始 +X 分别朝网格 -X / +X；导弹口初始 +X 朝网格 +Z。用户可以在模型编辑器骨骼树搜索 `FX_`，调整位置和旋转，保持 +X 指向实际出口方向。`FX_AimTarget` 用于表示他人攻击本机甲时的视觉目标位置，通常放主体中心。

## 验证

- 新增 6/6；数量 FourF `0→2`、WM01 `9→13`；全部新父骨存在。
- 父骨局部→参考网格空间的位置和 +X 方向回读通过；旧 9 个 Socket 的父骨、位置、旋转、缩放全部未变。
- 两个资产明确保存成功并再次回读，最后脏内容包列表为空。
- Catalog 的全部发射点和 AimOffset 前后快照相同；未写 JSON、DataAsset、Crowd 或任何游戏代码。
- 源码编辑器 `AssetEditorSubsystem.open_editor_for_assets` 返回 true，已请求打开两份模型；没有把该返回值当成可视校准完成。
- 记录：`outputs/commander-rigs/adjustable-sockets-readback.json`，包含所有父骨局部及参考网格变换。
- 纯 Socket 资产操作，无需 C++ / Niagara 编译，未增加自动化测试文件。

## 遗留问题

- 用户负责精调并保存后通知；目前元数据 `CommanderRigSocketCalibration=PendingUserAdjustment`。不改用户后续摆放。
- `FX_` Socket 尚未接入战斗。当前机枪视觉终点读取 Catalog `AimOffset`；导弹、伤害和碰撞不读取这个新瞄准点。回填需要后续确认并正确转换坐标空间。
- WM01 武器重绑与两套 Rig 的完整可视验收仍未完成，本次创建挂点不代表该大任务已验收。
