---
schema: guli-progress/v1
id: ARC-20260917-002
work_id: WORK-20260916-006
kind: archive
role: root
title: Ship玩家蓝图补保存与独立读回
areas:
- ship
- rendering
categories:
- art
- gameplay
status: recorded
verification: passed
created: '2026-09-17'
updated: '2026-09-17'
summary: 用户关闭重复UE窗口后Ship玩家蓝图保存成功；独立进程重新加载并生成实例，确认新船体和描边引用、四套UV、原碰撞与55个挂点保留。
next_action: 原工作项继续机械姿态、真实轰炸和僚机死亡入口及性能验收。
relations:
  work_items:
  - WORK-20260916-006
status_note: passed仅覆盖Ship补保存与独立读回；整项开发仍为partial，不覆盖或改写上一阶段归档。
---

# 2026-09-17：Ship玩家蓝图保存阻塞解除

## 变更

用户关闭占用文件的重复UE窗口后，执行 `Scripts/install_stylized_ship.py`，编译并保存 `/Game/GuLiStrike/Ship/BP_CombatAvatarFly01`。文件于08:16写入，大小47,510字节。原Hull关闭绘制，AnimeHull和AnimeContour作为它的子组件使用已导入的动漫船体与独立描边；两个新组件不新增碰撞。

本次只补保存明确修改的玩家蓝图；未保存游戏关卡或会话开始前已有的三个脏Toon材质。主编辑器桥接已恢复，PIE已停止，临时LiveCoding设置恢复。没有修改C++或自动化测试文件，本次资产补保存不重复运行原生构建。

## 验证

证据位于 `ArtSource/StylePass_20260917`：

- `ship_install.json`：安装与保存返回success。
- `ship_after_save_live.json`、`ship_open_editor.json`：主编辑器读回组件层级，打开资产编辑器请求成功，蓝图不再脏。
- `ship_saved_readback.json`：源码引擎 `D:/UnrealEngine-5.7` 的独立进程重新加载磁盘蓝图与回退副本，并生成临时实例。新船体/轮廓引用正确、可见、父组件为原Hull、局部变换为单位变换、缩放1、NoCollision；轮廓不投影。原Hull网格、变换、QueryOnly碰撞及55个挂点保留，船体4套UV保留。全部检查通过，读回进程保存资产列表为空。
- 首次NullRHI读回在UE视口生成hit proxy时触发 `FSceneViewport::EnqueueBeginRenderFrame` 除零，未保存资产；日志为 `ship_saved_readback_nullrhi.log`。改为独立RenderOffscreen进程后08:19正常退出并通过读回，最终日志为 `ship_saved_readback.log`。不将首次失败记作通过。

## 边界与回退

这次只确认玩家蓝图修改已经落盘并可重新构造，不把它扩展为真实战斗、武器尾焰视觉、机械动作或性能验收。原计划的未验收项仍在开发文档保留。

Ship原版回退副本：`/Game/GuLiStrike/Ship/Stylized/Rollback/BP_CombatAvatarFly01_BeforeCel`。

关联：[上一阶段](20260917-三渲二模型与指定爆炸接入阶段记录.md)、[开发状态](../DevelopmentDocumentation/20260916-Ship导入与扫荡者战争机器风格重制.md)、[需求](../RequirementDocument/20260916-Ship导入与扫荡者战争机器风格重制.md)。
