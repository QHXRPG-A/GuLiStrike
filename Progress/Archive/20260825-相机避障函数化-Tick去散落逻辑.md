# 2026-08-25 解决了：相机避障逻辑提取为 ResolveCameraArmCollision()，Tick 去散落逻辑

- 对应开发文档：[DIY 飞船](../DevelopmentDocumentation/20260820-DIY飞船.md)（代码结构整理）
- 变更类型：C++

## 变更清单

| 文件 | 变更 |
|---|---|
| `Source/GuLiStrike/Gameplay/Ship/GuLiStrikeShip.h` | 私有方法声明 `ResolveCameraArmCollision()`（注释注明单写者、Tick 调用） |
| `Source/GuLiStrike/Gameplay/Ship/GuLiStrikeShip.cpp` | Tick 的相机避障段（原 280-360 行）整体移入新函数；Tick 只留一行调用；`if (SpringArm)` 包裹改为函数开头提前返回；逻辑逐行未动（纯搬移 + 降一级缩进） |

## 做了什么

应用户要求"相机避障封装成函数、Tick 不留散落逻辑"。这是此前"避障下放专职组件"评估的轻量替代：不动组件归属、不改数据流，只做函数化收拢——`DesiredArmLength`/`HullBoundingRadius`/各 Camera 参数仍为飞船成员，将来若真做组件化，这个函数就是现成的搬迁单元。Tick 现在读作：避障 → 偏航惯性 → 自动转向 → 压弯 → 清输入，五段一目了然。

## 验证

- 增量构建通过（8.2s）；
- PIE（LVL_ShipTest，BP_CombatAvatarFly01）：`springarm_target_arm_length=15000`——函数是臂长唯一写者，15000 说明 `ResolveCameraArmCollision()` 每帧正常执行且门控直通基线无回归；表值 `default_arm=15000`/`probe=2000` 不变。

## 遗留问题

- Tick 的偏航惯性/自动转向/压弯三段仍是内联代码（本次未动，需求只限相机段）；要同样收拢可另提。
- 组件化评估结论不变：出现第二个使用方（观战相机/非舰载具）时再做。
