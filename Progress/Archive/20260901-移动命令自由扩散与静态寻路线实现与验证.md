# 2026-09-01 解决了：移动命令自由扩散、单兵部分接受与静态寻路线

- 对应开发文档：[20260901-移动命令自由扩散与静态寻路线.md](../DevelopmentDocumentation/20260901-移动命令自由扩散与静态寻路线.md)
- 变更类型：C++ / Config / 测试 / 文档

## 变更清单

| 文件/资产 | 变更 |
|---|---|
| `Source/GuLiStrike/Commander/Mass/Navigation/GuLiCommanderDestinationPlanner.*` | 用1800cm世界轴六角格生成45000cm内2263个稳定候选；实现中心优先排序、9000cm软锚、确定性分配与组内匈牙利匹配，不再把固定5×5阵型作为终点合法性门槛。 |
| `Source/GuLiStrike/Commander/Mass/GuLiBattleAuthoritySubsystem.*` | 将移动命令改为候选投影、分配、路由、预约协调和固定步提交的scratch状态机；每帧最多64次候选投影和4次路径查询，支持共享路由、稳定拆组、单兵降级、单兵部分成功、失败预约恢复、结构化统计和最近移动诊断。NavMesh动态重建修复也改为同预算分帧执行，待修复成员暂时冻结。 |
| `Source/GuLiStrike/Commander/Network/GuLiCommanderTypes.*` | 协议升级为v6；Cohort ACK增加冻结成员数、Eligible掩码和Accepted掩码；完善掩码净化、部分接受聚合、显式拒绝原因保持及OwnerOnly活动终点FastArray。 |
| `Source/GuLiStrike/Commander/Framework/GuLiCommanderNetSyncComponent.*` | 接入异步规划Pending、可靠最终ACK、同请求幂等关联、选择版本等待、终点FastArray发布/Bootstrap和生命周期清理；本地权威路径也经过相同状态机。 |
| `Source/GuLiStrike/Commander/Framework/GuLiCommanderPlayerController.*` | 按Accepted成员掩码处理部分接受并等待ACK中的选择版本追上；Accepted反馈消费实际权威终点。后续A/B确认异步规划期间的位移预表现会先行再撤销，生产发令链已停用该位置预测。 |
| `Source/GuLiStrike/Commander/Presentation/GuLiCommanderHUD.*` | 绘制“权威提交起点→实际最终槽”的1px纯绿色静态直线；没有流动、箭头、脉冲、标签或淡入淡出，并按选中、存活、ActiveOrderId、屏幕裁剪和2px最短长度剔除。 |
| `Source/GuLiStrike/Commander/Framework/GuLiCommanderDebugCommands.cpp` | 增加`gs.GM.Commander.Nav.LastMove [CohortId]`，并让`Nav.Stats`汇总Pending、投影、路径、部分接受和结构化失败阶段。 |
| `Source/GuLiStrike/Commander/Mass/Navigation/Tests/GuLiCommanderDestinationPlannerTests.cpp` | 覆盖2263候选、450m边界、稳定顺序、自由/部分分配、间距、输入顺序不变性、方向回退和不足编组居中。 |
| `Source/GuLiStrike/Commander/Network/Tests/GuLiCommanderNetworkTests.cpp`、`Source/GuLiStrike/Commander/Framework/Tests/GuLiCommanderSelectionMoveGateTests.cpp` | 覆盖v6掩码、显式拒绝净化、终点FastArray、ACK/选择乱序、Pending串行与可靠重放。 |
| `Source/GuLiStrike/Commander/Presentation/Tests/GuLiCommanderMoveEndpointHUDTests.cpp` | 覆盖静态绿线生命周期判断和屏幕线段裁剪。 |
| `Config/DefaultEngine.ini` | 默认Game和Editor地图改为`/Game/Maps/LVL_CommanderMassPrototype`，Global GameMode使用原生`GuLiCommanderGameMode`。这避开`LVL_Main`、`LVL_ShipTest`遗留的Ship GameMode及缺失Commander导航数据。 |
| `Scripts/validate_commander_nav_config.py` | 扩充地图WorldSettings GameMode、Landscape边界、NavMeshBounds和NavData诊断，供默认地图配置复核。 |
| `Progress/DevelopmentDocumentation/20260901-移动命令自由扩散与静态寻路线.md`、现行玩法/Mass/网络文档 | 将方案状态、任务勾选、协议和移动规则更新为实际实现，并明确受限验证边界。 |

### 2026-09-01 发令拉回修正

异步自由落位上线后，历史的单兵客户端位移预表现会在服务器规划完成前先推进最多900cm，再固定撤销偏移。既有baseline/no_offset A/B已将纯表现反向位移隔离为该机制；生产发令链现已停用`BeginPredictedMove`和`ResolvePredictedMove`调用，只保留Pending反馈、权威姿态插值及静态绿线。

先用Live Coding验证后，`GuLiStrikeEditor Win64 Development`冷构建成功；重启编辑器确认只加载新的基础DLL，并再次用Standalone PIE真实鼠标链验证一兵移动：`mouse_input=1`、`input=0`、预测偏移0cm、显示与权威插值差0cm，ACK非零且单位实际移动。证据为[构建复核](../../outputs/build-pullback-fix-editor-20260901.log)和[`pullback-fix-validation-20260901.json`](../../outputs/pullback-fix-validation-20260901.json)。PIE已退出，编辑器已恢复打开。

## 做了什么

移动终点不再依赖点击中心或固定5×5块全部合法。服务器围绕点击点按二维距离从内向外消费六角候选，先过滤范围、投影修正、槽间距和未选友军硬预约，再用软锚维持同一Cohort的相对集中。Cohort仍承担路由和回执归属，但每个成员拥有独立的最终槽与接受结果；敌军不占用终点预约。

规划全过程在scratch状态中跨帧推进。Pending期间单位继续旧指令；候选准备好后优先构建共享长路径，失败则沿终点包围盒最长轴稳定拆组，最终降级到单兵路径。成员—槽位失败会尝试更外候选，只有候选耗尽才拒绝成员。提交前重新读取战局、导航代次、成员身份和权威起点，准备结果只在下一次30Hz权威步统一提交。

同一命令中的成功成员获得同一个非零`BatchOrderId`，失败成员保留旧指令并从当前选择中移除，选择版本整批最多推进一次。v6 ACK用冻结成员位序携带Eligible和Accepted掩码；重复快速请求与可靠回退关联同一Pending任务，完成后重放同一掩码、批次和选择版本。终点通过OwnerOnly FastArray独立收敛，避免把大量终点塞入ACK或10Hz姿态流。

客户端以权威终点绘制静态绿线。每条线固定1px绿色，只连接实际提交位置与实际自由槽；到达、Blocked、死亡、改令、取消选择或网络重置后立即消失。该实现删除了Accepted流水动画，本轮没有给绿线添加任何动效。

NavMesh生成代次变化不再在导航回调里同步遍历并查询全部活动单位。回调只建立稳定排序的修复任务并冻结受影响成员，随后由World Tick按每帧64次投影、4次规划路径的预算修复成员和Formation，下一固定步再提交；无法恢复的成员进入既有恢复/Blocked流程。

默认启动地图从`LVL_ShipTest`改为`LVL_CommanderMassPrototype`，并使用原生`GuLiCommanderGameMode`。检查显示`LVL_Main`和`LVL_ShipTest`仍绑定旧Ship GameMode，且`LVL_ShipTest`没有Landscape、NavMeshBounds或Commander NavData；继续以其作为默认PIE/无头入口会表现为没有士兵或无法规划。

## 验证

- `GuLiStrikeEditor Win64 Development`构建成功。
- `GuLiStrike Win64 Development`构建成功。
- 全量`GuLiStrike`自动化报告包含99个测试项，99项状态均为`Success`，失败0。报告摘要记为96成功，另3项带预期Warning；预期Warning没有变成失败。
- `GuLiStrike.Commander.Network`专项19/19通过，覆盖v6掩码、显式拒绝、终点FastArray和既有网络契约。
- Commander导航专项24/24通过，包含自由候选和既有表面移动/恢复回归。
- 默认地图无头运行实际加载`GuLiCommanderGameMode`并生成500名士兵。一次20名成员的命令生成2263个理论候选、1983个合法候选，统计为20/20 Accepted、2283次投影、1次路径查询、192次预约冲突，规划耗时46.832ms；探针成员移动3870cm，随后销毁20名目标，命令流最终PASS。
- 上述无头冒烟证明默认地图、500兵生成、一次20兵自由规划、移动和销毁链路可执行；它不等同于25/250/500兵交互PIE、双客户端或完整性能验收。
- 静态绿线源码与自动化通过；尚未采样500条线同时绘制时的运行耗时。
- NavMesh动态重建已在源码中按64投影/4规划路径的帧预算拆分；尚未用500个活动单位实测完整重建耗时、最坏查询量和固定步P95。
- Launcher版引擎不支持Server Target，本轮未完成Dedicated Server。Standalone双客户端也未执行，不能从单进程无头结果推断联机通过。

## 遗留问题

- 仍需在Editor内完成25/250/500兵交互PIE，覆盖空地、边缘、孔洞、友军密集区和起伏路面，并记录总规划帧数、查询峰值和权威固定步P95。
- 仍需采样500条静态绿线的绘制耗时，确认视锥/屏幕裁剪在实际视角下的成本。
- 仍需以500个活动成员触发NavMesh动态重建，核对分帧预算、冻结/恢复行为和最坏完成时间。
- 仍需完成Standalone双客户端、ACK/SelectionState真实乱序与重连终点恢复。Dedicated Server需切换到支持Server Target的源码引擎环境后验证。
