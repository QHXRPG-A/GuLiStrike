---
schema: guli-progress/v1
id: ARC-20260917-001
work_id: WORK-20260916-006
kind: archive
role: root
title: 三渲二模型与指定爆炸接入阶段记录
areas:
- ship
- commander
- combat
- rendering
categories:
- art
- gameplay
status: recorded
verification: partial
created: '2026-09-17'
updated: '2026-09-17'
summary: 两台手工单位增加内线、轮廓与三档明暗并接入数据表；三类指定爆炸副本已保存配置。Ship新资产已导入，玩家蓝图保存被重复UE进程占用阻塞。
next_action: 解除Ship蓝图文件占用后保存，继续机械表现和真实轰炸/僚机死亡入口及性能验收。
relations:
  work_items:
  - WORK-20260916-006
status_note: 阶段事实，不代表完整原计划验收；不新增或修改自动化测试文件。
---

# 2026-09-17：三渲二模型与指定爆炸接入

## 变更

| 交付 | 结果 |
|---|---|
| 扫荡者、战争机器 | 保留已有手工造型和橙红/暖白/蓝灰配色；2K结构线遮罩、深蓝外轮廓与三档固定艺术光向明暗；静态/骨骼模型位于 `/Game/Commander/Units/Tactical/Cel` |
| 远景LOD | 扫荡者10,912/2,727/900/220面；战争机器27,303/6,826/300/70面，均含轮廓；LOD0仍超早期预算 |
| 运行数据 | Soldiers两行模型引用更新；扫荡者/战争机器名称与Id1/2保留；WeaponMounts六行视觉坐标与静态网格Socket同步 |
| Ship | 新船体、轮廓、4K UV3线稿和六色材质已保存在 `/Game/GuLiStrike/Ship/Stylized`；玩家蓝图编辑器内编译通过但文件保存失败 |
| NS03/05 | 项目副本分别增加地面/空中冲击波，0.05～0.55秒，默认直径950/1050cm再乘范围参数 |
| NS01 | 项目副本三个径向扩散层水平位置/速度乘0.70，地面图形2500改1750；不叠第二套冲击波 |
| 配置 | 轰炸DA使用新NS01、scale5、User.Area_Scale、随机朝向、无额外层、期限5.25秒；摧毁INI使用新03/05；部署保留新变体 |

商城源资产只读，原火球/烟团曲线及完整消散节奏保留。面积与Burst粒子数解耦；组件缩放为1；原5000cm发射器裁剪改180000cm，Niagara池容量32，现有表现并发策略不变。战斗Id、伤害、范围和攻击节奏不改。

## 证据

完整交付说明为 `ArtSource/StylePass_20260917/README.md`。Blender交互文件为 `Models/Tactical_Cel_Review.blend`。UE模型预览地图为 `/Game/Commander/Units/Tactical/Cel/Review/LVL_CelModels_Review`，九张模型引擎图为 `UE_*_front/side/three_quarter.png`，特效阶段图为 `FXFrames`。

- `model_import.json`、`model_finish.json` 记录模型、UV、骨骼、材质、LOD与挂点读回。`soldier_table_import.json`、`mount_table_import.json` 的全部行读回匹配源表。
- `material_final_and_pie.json` 记录13个相关材质编译通过并分别保存；`fx_final_tuning.json` 记录三套Niagara最终编译零错误、零警告。
- `explosion_install.json` 记录DA引用/参数及未变的玩法字段；`explosion_rollback.json` 保留原INI段与先前变体。制作脚本Revision2为最终设置。
- 独立进程调用现有 `gs.WingmanAttack.QA.Start`，采用dedicated+two clients，未保存游戏关卡。`runtime_review.json` 记录63次采样；每客户端手动调用现有地面摧毁入口3次，峰值活动数3，最终归零，组件scale1、Area_Scale约7.55262。专用服务器未创建UnitFeedback和CombatEffectPresentation子系统，未创建这些爆炸组件。
- 真实轰炸入口没有观察到新NS01激活，僚机死亡入口未验证，因此这些不算实战通过。采样中的其它导弹计数不能当成僚机轰炸证据。首次记录收尾时编码别名错误，原始失败报告保留在 `runtime_review_first.json`，修正后独立采集脚本完成；局部验证边界不变。
- 源码引擎 `D:/UnrealEngine-5.7`：`GuLiStrikeEditor Win64 Development`、`GuLiStrike Win64 Development` 构建退出码均0。引擎、项目及UnrealMCP/UnrealMCPython/VibeUE/GuLiFlightNavigation/GuLiMapAuthoring的BuildId一致，为 `11b8900e-cd30-41cb-ad6b-96933e5d3584`，记录 `build_and_syntax_gate.json`。本轮未新改C++，构建包含此前字段改动和已有工作区修改。

## 保存阻塞与未验收项

- `BP_CombatAvatarFly01` 被另一同项目UE进程PID49428占用，保存返回Windows错误32。主编辑器内已隐藏旧Hull绘制、附加AnimeHull/AnimeContour并编译，但未能落盘。用户已被请求处理重复窗口；必须解除占用后执行 `Scripts/install_stylized_ship.py` 并读回保存，才能声明玩家默认外观完成替换。
- 主编辑器PID34116启动PIE后控制没有返回。发送了停止本次PIE/恢复临时LiveCoding开关的清理请求；编辑器未恢复前不能假定已执行。不强制结束含未保存工作的用户进程。
- 自动远景LOD已生成，但近景预算、100/500/1000单位成本和完整爆炸A/B GPU/CPU/透明覆盖均未验收。不凭减少粒子或新增LOD宣称提速。
- 轮子/机枪材质机械动画、死亡姿态冻结、完整HUD/阵营反馈仍属于原计划未完内容。本轮只完成本次风格与资源接入范围内的可保存资产。
- 未保存游戏关卡及会话初始的3个脏Toon材质；新模型预览地图独立保存。只修改明确目标资产。

## 回退

旧手工/原Crowd模型、商城特效和Big_17均保留。Ship回退副本在 `Stylized/Rollback/BP_CombatAvatarFly01_BeforeCel`。轰炸切换脚本 `Scripts/set_wingman_explosion_variant.py` 支持reference/toon/old；地面/空中原设置见 `explosion_rollback.json`。

关联：[需求](../RequirementDocument/20260916-Ship导入与扫荡者战争机器风格重制.md)、[开发状态](../DevelopmentDocumentation/20260916-Ship导入与扫荡者战争机器风格重制.md)。
