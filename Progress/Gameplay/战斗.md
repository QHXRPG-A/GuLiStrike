# 战斗

- 最近更新：2026-09-03（接入飞船独立GAS、僚机武器/导弹、统一Target Adapter与Damage Ledger；源码Dedicated、完整Server Cook及S9既有验收通过，Reward经济规则待配置）

## 模块概述

玩家在关卡中对抗敌人的所有行为：攻击、受击、技能、死亡与重生。位于核心循环的"挑战"环节，产出货币/进度等循环资源（待循环模块补充后互链）。

## 机制细节

> Commander Mass 小兵保留扫射与直接扣血；Ship/Wingman 已形成 Target Adapter、Fire Validator、服务器生命与幂等 Damage Ledger 的实现基线。Ground 正式战斗、护盾、科技树、卡牌和完整经济成长规则仍未完成。样例数值用于调试，不是平衡定案。

### 统一战局与三类玩家角色

- 一个战局 World 只运行一套服务器 GameMode。[GuLiBattleGameMode](../../Source/GuLiStrike/Battle/Framework/GuLiBattleGameMode.cpp) 统一管理战局、席位、角色 Pawn 出生与重生；Battle GameState 复制战局/席位，Battle PlayerState 复制玩家 GUID、队伍和角色，Battle PlayerController 承载公共连接。
- 旧 `AGuLiCommanderGameMode/GameState/PlayerState/PlayerController` 保留类名并派生自公共 Battle 类，兼容现有蓝图；这不是让三个 GameMode 在同一张地图并行运行。Commander、Ground、Air 的差异放在角色 Pawn 和专业组件。
- 角色在 `GenericPlayerInitialization` 分配，覆盖正常登录、失活 PlayerState 恢复和无缝切图的初始化入口；服务器按 `InitialRolePriority` 和同一张席位表选择，客户端不能自行认领身份。此处说明代码路径，不代表重连/无缝切图已完成实测。
- 当前组合配置为 Commander 相机、Ground 原生占位 Character、Air 的 `BP_CombatAvatarFly01`。`RolePawnClasses` 缺项、抽象类或非法配置会记录错误、释放席位并转观察者，不会偷用其他角色的 Pawn。测试预设可调整初始角色优先级，但仍经过席位校验。

### 就绪条件与通信分工

- 公共连接由 [GuLiPlayerNetSyncComponent](../../Source/GuLiStrike/Battle/Network/GuLiPlayerNetSyncComponent.cpp) 确认当前协议、玩家身份、角色席位和当前 MatchEpoch/连接代次；服务器确认后置 `BattleReady`。它只表示玩家加入当前战局的公共身份已同步。
- 士兵专业流还要由 [GuLiCommanderNetSyncComponent](../../Source/GuLiStrike/Commander/Framework/GuLiCommanderNetSyncComponent.cpp) 等待本局名册/快照就绪，才置 `SoldierStreamReady`；旧 `IsSyncReady` 仍是这个士兵位的兼容接口。公共连接就绪不能放行旧战局姿态、士兵命令或半套名册。
- [GuLiCommanderWorldReplicationComponent](../../Source/GuLiStrike/Commander/Framework/GuLiCommanderWorldReplicationComponent.cpp) 挂在当前权威 GameMode，开启士兵模拟并统一调度离散快照与姿态发送；组件自身不是 RPC 对象。玩家载具移动不走士兵 PoseChunk，Ground 使用 CMC，Ship 使用其派生移动组件。
- Ground 操作依赖本地拥有者、Ground 角色和公共连接就绪，不等待士兵名册；Ship 还要通过自己的装配/移动配置屏障。角色或连接代次变化会撤销旧就绪状态。

### Ground 战争机器占位

- 当前落点是 [GuLiWarMachinePlaceholderPawn](../../Source/GuLiStrike/Gameplay/WarMachine/GuLiWarMachinePlaceholderPawn.cpp)：Character 胶囊碰撞、引擎 Cube 外观、WASD 移动与鼠标相机；移动预测、服务器校正和远端显示复用 CharacterMovement。静态外观挂在 CharacterMesh0 下，跟随 CMC 网格平滑。
- `CanFire()` 与 `RequestFire()` 明确返回 false，不发送武器 RPC。当前没有战争机器模型、攻击、伤害、技能或自定义载具物理，不能把可走动占位角色描述为完整战争机器玩法。

### Pawn 销毁后的公共重生

- 服务器真正销毁当前已占有的 Pawn 后，公共 PlayerController 通知 Battle GameMode，统一通过 `RestartPlayer` 重生；保留该玩家 GUID 和席位。默认开启，延迟 0 秒表示下一 Tick，避免在 Pawn 销毁栈内立即生成。
- 退出、切图、比赛结束/中止会取消待执行重生；普通取消占有或 Controller Reset 不等同于死亡触发。该流程沿用 AGameMode 比赛阶段，没有新增胜负条件。
- Ship 已通过自定义服务器 Health Component 接入死亡链：生命归零后取消旧 Ship ASC、使旧 GroupAbilityConfig 失效并结束旧僚机组；公共重生创建新 Ship、新 ASC、新 GroupGeneration 和新僚机身份，只从 PlayerState 恢复稳定 AbilityId，旧活动实例与冷却不继承。
- Commander Soldier 继续使用既有 Mass 生命；Wingman 生命属于服务器生命周期数据。护盾、玩家死亡惩罚、胜负条件和具体经济奖励仍未定案，Reward 目前只有 Policy/Sink 接口。

### 飞船与僚机攻击、伤害和死亡

- 飞船自身物理武器仍由 Air 拥有者提交按下/松开意图，[GuLiStrikeShip](../../Source/GuLiStrike/Gameplay/Ship/GuLiStrikeShip.cpp) 在服务器校验 Pawn、Air 身份、BattleReady、比赛阶段和配置屏障；已装武器按各自射速生成服务器复制弹丸。现有开火链没有迁移成 GA，但弹丸命中已通过窄桥接进入统一 Target Adapter 与 Damage Ledger。
- 初始自动武器是 Ship 长驻被动 GA 对整组僚机发布的授权。每架存活 Wingman 在 Lease Owner 客户端独立索敌并维护自己的冷却、`DomainFireSequence`、Emitter 身份与 `SourceAcceptedStateRef`；服务器逐次验证版本、Accepted 状态、敌我、距离和 LOS 后才允许结算。每一发不是一次 GA 激活。
- 主动导弹齐射由鼠标右键触发。客户端只预测准星锁定反馈；服务器从准星前向锥体选择最近合法目标和一个具有新鲜 Accepted 状态的稳定 Flight，每个合法存活成员发射一枚。没有合法发射者时整次失败且不进入共享冷却。
- 导弹是服务器 30Hz 轻量逻辑记录，以 Segment/Sphere Sweep 命中，不生成逐弹复制 Actor；客户端根据 Launch/Correction/Impact 事件重建表现。导弹发射后不换目标，目标死亡、超时或路径失效时自毁；已接纳导弹可在 Emitter 随后死亡或组进入 Stale 后完成一次既定结算。
- 基础武器、逻辑导弹和飞船物理弹丸最终进入同一幂等 Damage Ledger；重复 Shot/Impact/Damage ID 最多提交一次伤害和一次死亡。Wingman 死亡从可靠 Roster 移除，母舰存活且组恢复 Active 后每15秒补充1架；Ship 死亡则整体结束旧组。
- 当前默认 Air 子蓝图 `BP_CombatAvatarFly01` 没有 socket 和默认装配，因此不会产生“飞船自身物理武器”弹丸；这不影响 Ship ASC 授予的僚机编队、基础武器和主动导弹能力。

### Ship GAS 与能力来源

- PlayerState 上的 Army ASC 只承载指挥官军队规则；每个 Ship Pawn 另有一个 Mixed 复制的 ASC，`OwnerActor=AvatarActor=Ship`。Ship 与单架 Wingman 均没有新增 `AttributeSet`，生命、速度和伤害不由 GameplayEffect 改写。
- 首版稳定 AbilityId 为双环编队、初始自动武器和主动导弹齐射。前两项由服务器自动激活，导弹使用 `InputTag.Ship.Wingman.Missile`；任一 Active 组必须有且只有一个有效编队能力。
- Ship GAS 只负责授权、激活、互斥、取消和共享冷却，并把结果可靠投影为 GroupAbilityConfig；逐架编队、导航、避障、索敌和 FireIntent 仍由当前 Lease Owner 的客户端 Mass 执行。Backup Owner 可消费投影恢复模拟，但不能访问原 Ship ASC 或释放主动导弹。
- 飞船数值继续由基础表、部件和修饰源合成，并只经 `RecomputeStats`/CMC 配置屏障提交；GAS 不成为第二套数值真相源。
### 小兵普通攻击：扫射

- `Strafe` 显示名为“扫射”，分类标签为普攻、通用；两兵种共用 `DirectSingleTarget` 执行器，各自从兵种技能表读取伤害、次/秒和厘米射程。默认500兵中红蓝各250，每队A、B各125。`StrafeTest` 仅为同执行器测试变体，不代表火炮已经开发。
- 单位在停止、移动和导航恢复期间都自动攻击射程内一个敌方 Mass 小兵，不为攻击追击。合法目标保持；目标死亡或超出三维中心距离后失效，每200ms按SoldierId分批重选最近目标，等距按SoldierId。每次射击前复核目标。
- 活动移动命令不再产生`Moving`停火。移动、恢复与到达状态变化不会清目标或刷新冷却；无合法目标时不累积补射。每兵独立冷却，换目标不重置冷却。攻速改变保留剩余冷却比例，替换主体技能清除旧目标并等待新技能完整冷却。
- 有效攻速0～30次/秒，0停火；无墙体/地形遮挡检测，无实体子弹、正式特效、动画和音效。表现可通过血条、死亡残骸及GM诊断观察。
- 每个30Hz权威步先完成移动/到达更新，再按移动后的三维位置采样攻击，最后统一直接扣血，允许互杀。没有防御、暴击或命中率公式；Defense字段保留但不参与扫射结算。生命和单次伤害全链使用float，支持300.5生命、7.5伤害。死亡只处理一次，沿用隐藏兵模、暗红脚环保留5秒的表现。

### 指挥官 GAS 与技能来源

- 公共 `BattlePlayerState` 持有ASC，仅指挥官获得服务器军队能力。中性GM命令实际经过ServerOnly GA进入World子系统；Mass热路径不访问ASC，客户端不结算伤害、不逐发接收RPC。
- 来源账本归属当前World/Team，不归属Pawn或ASC宿主。重复SourceInstanceId为整条更新；移除来源重新解析当前剩余状态。指挥官席位、队伍、当前Controller及公共/士兵流两道就绪门均在服务器复核；重绑ASC不重复添加来源。
- 槽是可扩展FName，本期执行`BasicAttack`，每槽一个主体技能。先按优先级决定最终技能，再取该兵种/该技能基础值，最后按最终技能ID或标签筛选修改。所有优先级层的同优先级不同技能冲突均拒绝整笔变更，替换不连锁推演。
- 数值为`(基础值 + 固定加值总和) × 各来源(1 + 百分比之和)的乘积`。同一来源内部百分比相加，不同来源相乘：两个独立+20%为×1.44。槽通用修饰跨技能保留，技能专属修饰随最终技能筛选。
- 修改在下一权威模拟步统一生效，最终配置带Revision和MatchEpoch复制。已存在和新生单位读取相同最终配置。新World/战局清空来源，旧局快照不会跨MatchEpoch暴露。
- 新攻击通过执行器注册接入；同攻击行为的变体只增加配置。附加效果仅保留注册/调用接口，本期没有燃烧、穿透、科技树或卡牌适配器。

### 表格与 GM

- `GuLiStrikeCommander.xlsx` 分为Soldiers（生命/移动/模型/保留防御）、Skills（身份/标签/执行器）、UnitSkills（兵种+槽+技能参数/默认装备），沿用Excel→生成结构→JSON→DataTable。旧兵种表攻击力与射程列已移除，避免双重数值来源。
- 服务器非Shipping使用`gs.GM.Skill.List/Get/Set/Reset/Source/Replace/Remove/Soldier/Spawn/Bench`。`Set`在正常解析后直接覆盖，`Reset`只清数值覆盖；均不写Excel。完整参数与示例见[开发方案](../DevelopmentDocumentation/20260901-小兵扫射与指挥官GAS桥接.md)。
- 最大生命GM沿用`gs.GM`，存活者按原生命比例换算，死亡者保持0。旧`soldier.attack_power/attack_range`给出迁移提示。数值非法、非有限或超界时拒绝并保留完整旧状态，不静默限幅。
- 士兵协议升为v5，可靠状态直接同步float当前/最大生命；旧v4不可混连。

### 本轮导航与移动射击验证边界

移动射击、移动后采样攻击的代码已通过Game Development、DebugGame Editor和Development Editor构建；DebugGame全量`GuLiStrike`89/89、当前Development导航组20/20通过。GUI Editor已实际加载当前Development DLL，但尚未完成交互PIE；移动中完整射速、目标保持、同归于尽及Dedicated双客户端结果仍待实测。详见[实现与验证边界归档](../Archive/20260901-指挥官选兵移动导航与小兵GAS扫射总归档-0831至0901.md)。

### 2026-09-03 飞船与僚机验证边界

- `GuLiStrikeEditor Win64 DebugGame`最终重编译60/60 actions成功。最新自动化：Wingman 72项全部`Success`（66成功、6项带受控StateTree fallback/Nav Gate夹具警告）、Ship 6/6、Combat 12/12、FlightNavigation 14/14，全部0 failed、0 notRun。
- Normal、Weak、Reorder三个70秒服务器/50秒客户端的真实双进程Listen短测均`passed=true`：六Scope、逐Flight接纳、原子事务、客户端尾部五Flight持续推进及Relay权威运动写入为0。Weak实际经历RosterRevision `1 -> 6`。
- 后续已在源码UE 5.7.4完成Development Game/Server、完整Server Cook、20个真实Dedicated角色和2个拆分H4000角色；S9 Control/Feature各10分钟、4舰、500 Commander与0/100 Wingman均通过既有acceptance，已设不变量为0。未追加每子系统P95、Control→Feature回归和Private Bytes增长机器判定，因此这些数值阈值不声明通过；Ground六向交互仍属外部模块前置。详见[实现与既有验收总归档](../Archive/20260903-飞船GAS僚机体系与三维导航实现与既有验收总归档.md)。

### 历史验证记录（扫射与GAS桥接）

本期最终编译及79项自动化通过。Dedicated+双客户端通过来源、替换、浮点血量/死亡和权限检查；500兵20秒交战累计12,694次射击、462阵亡，双客户端死亡数量一致。万人驻停/测试对样本权威固定步均值23.582ms，整轮峰值52.564ms，完整性能未判定达标。完整范围、过程异常和原始报告见[本期归档](../Archive/20260901-指挥官选兵移动导航与小兵GAS扫射总归档-0831至0901.md)。下节为之前公共战局框架的历史验证，不能替代本期证据。

<a id="本轮验证边界"></a>

### 历史验证边界（2026-08-31公共战局框架）

撤回额外补 ACK 重写后，最终冷编译耗时 14.17 秒；全量 GuLiStrike 自动化测试 50/50 成功，exit 0。Listen Server 指挥官身份、名册及同 GUID/席位重生已通过。

原客户端使用 open 重新连接后 GUID、席位不变。Dedicated Server + 2 个独立 -game 客户端通过原生 Timer 调用 RestartGame，同图无缝重建后 MatchId 更新、原 GUID 与槽位 0/5 保留，两道就绪门均通过。Listen Server + 1 客户端通过原生 DEFER servertravel 完成“原型图→ShipTest→原型图”往返，Controller 为 Native→BP_PC_Ship→Native，身份不变；ShipTest 无导航时名册未就绪、公共就绪为 true，回原型后两门均为 true。

混合角色测试最终为 10 名玩法玩家（2 Commander、4 Ground、4 Air）和 2 名观察者，12 个 GUID 唯一；全员 BattleReady、SoldierReady，4 艘 Air 飞船均 ShipReady。两名观察者分别来自出生碰撞和真正满席；只在运行时分开占据出生点的船体，未保存地图。

Ground 在 Dedicated 混合会话中注入 10 秒 AddMovementInput，81 个采样确认 MoveWalking 双端可见。服务端销毁后 Pawn_4→Pawn_7、GUID 与槽位 6 保留，最终位置差约 0.065cm；鼠标光标关闭，CanFire、RequestFire 均为 false。同帧销毁并 EndMatch 后 Pawn 为空，排队复活取消通过；这是输入注入验证，尚非实际按键的完整端到端测试。

Ship 的原生 Timer 弱网销毁复活保持 GUID/席位，恢复后位置差最大 9.40cm；NativeTick RPC 路径下换装、GM 倍率 1.3 和 17 枚服务端/客户端弹丸一致，松键后计数停止增长。原生 Timer 失占有后持续输入也不再开火，且未误复活。采样范围见[飞船验证边界](飞船.md#本轮验证边界)。

NetworkGate 尚未整体 PASS：旧三轮分别为后台节流导致无效、ACK 指标 159.9ms、ACK 指标 146.4ms 但 snaps=3。第 4 轮有效 DEFER 复测为 ACK P95=138.1ms、入站平均/P95=0.459/0.467Mbps、gap=384.9ms、硬跳变 1 次，仍保留失败结论。

此前直接在 Python/Slate 中触发的同步切图、占有 RPC 受 FEditorScriptExecutionGuard 强制本地执行；受影响的旧失败和重占有结论均不作为原生联机证据，也不归因于 UE 缺陷。补 ACK 重写已撤回。完整实体内容、FullWASD 实际按键与主观画面体验仍未全面覆盖。

本轮完整记录见[正式归档](../Archive/20260831-公共战局框架与三类角色接入.md)。

## 数值速查

| 数值 | 值 | 备注 |
|---|---|---|
| 玩家生命 | 待定 | |
| Ground 占位移速 | 600cm/s | CMC MaxWalkSpeed，不代表最终战争机器性能 |
| 玩家 Pawn 重生延迟 | 0s（下一 Tick） | 默认开启；可配置，仅比赛进行中有效 |
| 敌人生命 | 待定 | |
| A：生命 / 伤害 / 攻速 / 射程 | 100 / 10 / 2次每秒 / 100m | Strafe调试初值，非平衡定案 |
| B：生命 / 伤害 / 攻速 / 射程 | 300.5 / 7.5 / 4次每秒 / 150m | 同执行器，浮点生命和伤害 |
| 扫射攻速上限 | 30次/秒 | 0停火；超界变更拒绝 |
| 索敌周期 / 权威步 | 200ms / 30Hz | 分批索敌，每次开火复核 |
| 每舰僚机编制 | 25架 / 5个Flight / 每Flight 5架 | 2026-09-03新增首版配置 |
| 僚机基础武器 | 伤害10 / 冷却2s / 射程1500m / 前向半角20° | 新增资产默认值，非平衡定案；每架独立冷却 |
| 僚机导弹齐射 | 伤害100 / Ship共享冷却8s / 射程2500m / 锁定半角8° | 新增资产默认值，非平衡定案；每个合法Flight成员一枚 |
| 逻辑导弹推进 | 30Hz / 复制Missile Actor=0 | 服务器Segment/Sphere Sweep，客户端事件表现 |
| Wingman补充 | 每15s补1架 | 仅母舰存活且组为Active时实际生成 |

## 演进记录

- 2026-08-16：创建文档骨架
- 2026-08-28：Commander Soldier 最大生命、攻击、防御和射程进入表驱动与权威 Stats；明确实际攻击逻辑仍不在本轮范围。

- 2026-08-31：公共 Battle 框架承接角色、席位、连接和 Pawn 生命周期；士兵同步改由专业组件调度。新增 Ground 可移动占位及飞船服务器装配/开火边界，明确双就绪门与尚未实现的伤害消费者。
- 2026-09-01：新增扫射、多兵种技能配置、公共ASC军队能力和World来源桥接；生命/伤害迁移float，协议v5；科技树与卡牌不在本期。
- 2026-09-01：扫射从“持有移动命令停火”改为移动和导航恢复期间持续开火；固定步顺序明确为移动/到达→攻击采样→统一结算。Game/两种Editor构建、DebugGame全量89/89及Development导航20/20已通过；GUI Editor已加载当前DLL，实际射速与联机表现仍待交互验证。
- 2026-09-03：新增Ship Pawn独立ASC及双环编队、初始自动武器、主动导弹齐射三项能力；Wingman基础武器、逻辑导弹和Ship物理弹丸统一接入Target Adapter与幂等Damage Ledger，补齐Ship/Wingman死亡和15秒补充。随后完成源码UE 5.7.4 Game/Server、完整Server Cook、20个真实Dedicated角色、2个拆分H4000角色与S9双10分钟既有验收；Reward经济规则待配置，Ground前置和未追加机器判定的性能数值阈值不计为已通过。
