# 指挥官

- 最近更新：2026-08-28（Soldier 表驱动、松散目标到达、远距点选、heading-up 小地图、运行时 GM 调参与指令术语统一）

## 模块概述

指挥官从大地图视角观察战场，通过圆形范围临时组织附近友军并下达移动指令。玩家能够操控的最小组织粒度是最多 25 名小兵，但这 25 人不是永久编制：每名 Soldier 始终独立，下一次选择可以重新组合。

当前原型生成红蓝双方各 250 名 Soldier，共 500 名。服务端裁决身份、选择、单位指令、移动、避碰、生命和伤亡；客户端负责即时输入反馈、平滑显示、小地图和指挥 UI。

## 机制细节

### 圆形选取与动态 ControlCohort

- 触发：按 `1/2/3` 选择 80m、200m 或 450m 半径，左键点击地面；按住 Shift 左键使用整组 Toggle。
- 流程：客户端只提交圆心、半径档、Replace/Toggle 和 Revision，不提交 SoldierId。服务端从圆内存活、同队、可控 Soldier 中取种子，以动态质心确定性聚合最多 25 人；最后一组欠编时可从圆外 300m 内补员。
- 空地：圆内没有有效种子时不从圆外凭空拉人。
- 欠编：可用友军不足时允许少于 25 人。
- Replace：重新生成当前所有临时组。
- Shift/Toggle：命中既有 Cohort 任一成员即移除整个 Cohort；保留组不重排，未被旧组占用的新兵另组。
- 冻结：成员在当前 SelectionRevision 内冻结；死亡不补员；全灭后移出选择。
- 反馈：选中 Soldier 脚环为金色，未选为队伍色；底部显示 CohortId、存活数/25、欠编和指令状态。

### 移动指令与 OrderFormation

- 触发：已有已确认选择时右键点击地面。
- 流程：每个选中 Cohort 在服务器上复制为独立 OrderFormation；同一批次的所有 Formation 使用同一个 NavMesh 投影目标。最新被接受的移动指令覆盖重叠 Soldier 的旧 ActiveOrderId，旧 Formation 剩余成员继续前进。
- 选择与指令解耦：重新选择、Clear 或 Esc 不会停止已经接受的旧移动指令。
- 路径：每个 Formation 共享一条 `CommanderSoldier` NavMesh 路径，不为 25 人分别寻路。
- 阵形：途中依据可走宽度在 1–5 列间弹性调整；真正进入最后接近阶段后取消槽位修正，不再恢复 5×5，由 Mass Avoidance 和局部分离在目标域内自然铺开。
- 碰撞：服务端使用 7.5m Mass AgentRadius、Mass Avoidance 和局部分离；不使用 500 个 Chaos 刚体。
- 到达：到达域按本批移动指令的初始接受人数计算；25/50/100 人半径约为 50m/65m/95m。同批次须等待引导点、末弯和所有仍存活且仍跟随本指令的成员共同满足后统一结束；死亡或改令成员不阻塞。
- 拒绝：目标无法在一个 AgentRadius 内投影时返回 `InvalidTarget`；路径无效或 Partial 时返回 `PathFailed`。
- 反馈：命令线 Pending 为橙黄、Accepted 为青绿、Rejected 为红色；三态均带流水箭头和目标标记。

### 双端权威与即时表现反馈

- 服务端以 30Hz 模拟每名 Soldier，是位置、速度、朝向、指令、碰撞、生命和伤亡的唯一权威。
- 服务端以 10Hz 发送逐兵 PoseChunk；可靠 FastArray 单独同步身份、队伍、生命和 ActiveOrderId。
- 客户端默认在服务器时间后 100ms 渲染，用带速度的 Hermite 插值位置、最短弧插值 Yaw；缺帧最多外推 100ms。
- 点击移动后立即进行最长 250ms 的表现预测；最大距离由复制的有效 Soldier 移速推导，默认 `3600cm/s × 0.25s = 900cm`。拒绝或权威 Pose 到达后用 150ms 融回。
- Teleport 或误差超过 10m 才允许硬校正；预测不参与选取、碰撞、命中或伤亡。
- 无动画：FourFRobot 使用静态代理，但移动时持续更新位置、Yaw 和地面贴合。

### 指挥官 UI 与战场反馈

- 相机：WASD/屏幕边缘平移、Q/E 旋转、滚轮缩放；透视相机 Pivot 跟随 Landscape，不是固定高空俯视。
- 小地图：右上角 Panel/UI 固定，Landscape 简化高度、坐标轴、红蓝兵点、金色选中点和相机视野框随玩家摄像机反向旋转，使镜头前方始终朝上；左键通过严格逆变换聚焦，旋转后的战场外空角不会跳转。
- 选区：左下角三档按钮与鼠标位置上的半透明圆形轮廓。
- 单位脚环：未选为队伍色，选中为金色；Soldier 摧毁后模型隐藏，暗红脚环保留 5 秒后隐藏。
- 底部卡片：按当前临时 Cohort 事件驱动增删，显示存活数、欠编、等待、移动、拒绝或全灭状态。
- 输入保护：小地图和底部卡片区域阻断 LMB/RMB 世界命令，避免 UI 点击穿透。

### Soldier 数据与运行时 GM

- 当前 500 人统一使用 `DefaultSoldier`。移速、最大生命、静态模型、攻击力、防御力和攻击范围来自 `GuLiStrikeCommander.xlsx / Soldiers`，经 JSON 与 UE DataTable 导入。
- 每个 Game/PIE World 初始化时解析一次；缺表、缺行、非法值或非 `UStaticMesh` 模型会回退安全默认值，不在 Tick 查表。
- 非 Shipping 构建可用 `gs.GM.List/Get/Set/Reset` 在当前 World 会话中覆盖 Soldier 与飞船白名单数值；Override 不写回 Excel、DataTable、ini 或磁盘。
- Soldier 移速在下一次 30Hz 服务端权威步应用到 500 个 Mass Entity；最大生命按原生命比例换算，死亡单位保持 0。
- 攻击力、防御力和攻击范围已经进入权威 Soldier Stats，但当前没有攻击 AI、目标选择或伤害公式消费这些字段。

## 数值速查

| 数值 | 值 | 备注 |
|---|---:|---|
| 原型 Soldier 总数 | 500 | 红蓝各 250 |
| ControlCohort 容量 | 25 | 最小控制粒度，允许欠编，非永久编制 |
| 选择半径 | 80m / 200m / 450m | 1/2/3 三档 |
| 选择空间哈希 | 100m | 服务端权威查询 |
| 自动补员上限 | 300m | 每次加入后按新质心重新计算 |
| 服务端模拟 | 30Hz | 固定步 |
| Soldier 移速 | 36m/s | `DefaultSoldier` DataTable 基线，可由 World GM Override 临时覆盖 |
| Soldier 转向 | 90°/s | 当前原型值 |
| Commander AgentRadius | 7.5m | 服务端 Mass/Nav Agent |
| 到达方式 | 松散目标域 | 不重组最终方阵；25/50/100 人约 50m/65m/95m |
| 途中阵形 | 1–5 列 | 按走廊宽度选择 |
| Pose 捕获 | 10Hz | 每 3 个服务端固定步 |
| 插值回看 | 100ms | 客户端表现时间 |
| 最大外推 | 100ms | 之后保持 |
| 硬校正阈值 | 10m | 或显式 Teleport |
| 表现预测 | 250ms / 默认 9m | 距离为有效移速 × 时长，不具备玩法权威 |
| 预测收敛 | 150ms | ACK/Pose 后融回 |
| 残骸脚环 | 5s | 模型摧毁后隐藏，仅脚环保留 |
| 局部流场 | 默认关闭 | 500cm 单元、64×64；待专项验收 |

## 演进记录

- 2026-08-27：初版 500 人 Commander Mass 原型形成，随后修复 HISM/周期性重建造成的频闪和场景过暗。
- 2026-08-27：澄清“25 人一组”为动态最小控制粒度；永久 CommandGroup 迁移为 SoldierId、临时 ControlCohort 和 OrderFormation。
- 2026-08-27：由 5Hz 组摘要直驱表现迁移为 10Hz 逐兵 Pose、Hermite 插值、外推和短时预测；保持服务端权威。
- 2026-08-27：接入 CommanderSoldier 共享 NavMesh、弹性 1–5 列、5×5 终点足迹、Hungarian 槽位与 Mass Avoidance。
- 2026-08-27：接入可靠 Bootstrap/FastArray、Fast Intent + Reliable fallback、受损网络 Gate、稳定 ISM、脚环、小地图、动态 Cohort 卡和流水命令线。
- 2026-08-27：功能与受损网络平滑基线归档；性能、复杂导航、500 人同时移动、seamless travel 和流场默认开启仍待专项验收。
- 2026-08-28：`DefaultSoldier` 改为 Excel/DataTable 驱动，基线移速 18m/s → 36m/s；模型、生命和战斗 Stats 使用同一运行时定义。
- 2026-08-28：远距 Landscape 射线扩展到 600000cm；移动改为同批共享目标与松散到达，不再验证/恢复终点 5×5 方阵。
- 2026-08-28：小地图内容改为 heading-up；新增 7 个 World 会话级 `gs.GM.*` 调参 key，并同步 Soldier 预测与飞船 `GM.Runtime` 修饰层。
