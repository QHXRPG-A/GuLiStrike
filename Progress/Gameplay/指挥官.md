# 指挥官

- 最近更新：2026-09-01（移动规划改为按人数投射确定性候选前缀并同帧推进；2263仅保留为异常回退上限）

## 模块概述

指挥官从大地图视角观察战场，默认通过左键单选或拖拽框选精确选择友军，也可切换圆形范围选取并下达移动指令。一个 Soldier 可以单独选中；最多 25 人的 ControlCohort 只是服务器内部临时分组容量，不是最小选择人数，也不是永久编制。

当前原型生成红蓝双方各250名Soldier，每队A、B各125名，共500名。服务端裁决身份、选择、单位指令、移动、避碰、扫射、生命和伤亡；客户端负责即时输入反馈、平滑显示、小地图和指挥UI。

## 机制细节

### 公共 Battle 框架与士兵专业组件

- 指挥官、Ground 和 Air 共用 [GuLiBattleGameMode](../../Source/GuLiStrike/Battle/Framework/GuLiBattleGameMode.cpp) 的战局、身份、席位、角色 Pawn 分配与销毁后重生。旧 Commander 框架类名保留为兼容派生类；Commander GameMode 现在主要组合士兵发布组件、旧 UI 和三个角色的 Pawn 配置，不再把公共登录/重生逻辑专门写死为指挥官。
- 每名玩家仍只有一个名为 `CommanderNetSync` 的默认连接子对象：[GuLiCommanderNetSyncComponent](../../Source/GuLiStrike/Commander/Framework/GuLiCommanderNetSyncComponent.cpp) 继承 [GuLiPlayerNetSyncComponent](../../Source/GuLiStrike/Battle/Network/GuLiPlayerNetSyncComponent.cpp)，在公共身份握手上追加名册、选择、命令 ACK、重试、限流和姿态 RPC。旧名字为蓝图/子对象路径兼容保留，不表示所有角色都获得指挥权限。
- 公共 `BattleReady` 确认本局身份/席位/连接代次；专业 `SoldierStreamReady` 还要求本局 FastArray 名册、快照及专业同步代次匹配。旧 `bSyncReady`、`IsSyncReady()` 仍指士兵流，不能用公共就绪替代。角色变更、身份恢复或迁移后的旧就绪位会清除并重新确认。
- 发出指挥命令必须同时满足 Commander 身份、公共连接与士兵流就绪；服务器仍独立复核权限。其他角色可通过当前组合中的士兵接收通路观察战场，但不因收到 Soldier 状态获得选择/移动权。
- [GuLiCommanderWorldReplicationComponent](../../Source/GuLiStrike/Commander/Framework/GuLiCommanderWorldReplicationComponent.cpp) 是挂在权威 GameMode 上的服务器本地组件，负责启停士兵模拟、确保 Replicator/Presentation Actor，以及统一捕获快照、分摊姿态块和逐连接发送。它不承载 RPC；Pose 的压缩分块仍由 Authority 生成，实际 Client RPC 仍在专业 NetSync。
- 一个 World 只允许当前权威 GameMode 上的首个发布组件调度，避免蓝图误重复添加造成重复发送。没有该发布组件的公共战局不必启动 Commander Mass 士兵模拟；结束时也会关闭它持有的模拟开关。

### 精确选兵与动态 ControlCohort

- 默认：工具为“选取”、形态为 Box。左键按下记录位置与 Shift/Alt，松开后提交；移动不足 6 个逻辑像素视为点选，达到阈值视为框选。逻辑阈值按当前 viewport DPI 缩放，框选四个拖动方向一致。
- 形态切换：按 `7` 或点击原选取槽在 Box/Radius 之间往返，按钮图标与提示同步；移动工具结束后保留之前的选择形态。切换形态取消尚未提交的拖拽。
- 范围：Radius 下按当前半径选取实际命中的存活友军。仅在“选取工具 + Radius”时，数字键盘 `Add` 或主键区 `Shift+Equals` 按 `80→200→450→80m` 循环；裸 `Equals`、Box 和移动工具不改变半径，底坞没有 `+` 按钮。
- 精确命中：点选传单个 `SeedSoldierId` 提示及点击射线，服务器复核存活、队伍和有界身体/射线误差；无效提示直接拒绝，不改选邻兵。框选传共用相机原点的四角视锥，服务器按脚点判断。客户端不提交最终成员列表。
- 不补员：点选、框选、范围选取全部只选实际命中者，不再补满 25 人。空区域普通选择清空，Shift 空区域保持原选择；Alt 未点中有效士兵不改变选择。
- Shift：`Shift + 点选 / 框选 / 范围` 是纯 Add，保留原选择并加入新命中成员；重复命中不移除、不重复计数。并集改变时整体按最多 25 人稳定分组，避免大量单兵追加过早耗尽 400 组；集合不变时不重建、不增加选择版本。旧 Toggle 只保留内部兼容入口，玩家 Shift 不使用它。
- Alt：按住 `Alt + 左键点兵`，以该士兵为中心选择同队、存活、同兵种单位；全地图按二维距离从近到远排序，等距离按 SoldierId 排序，本次候选最多 1000 人。全图不足则选实际数量；`Alt + Shift + 左键` 将这批候选追加到已有选择。1000 不限制普通选取或 Shift 累计人数。
- 冻结：成员在当前 SelectionRevision 内冻结；死亡不补员；全灭后移出选择。每组仍最多 25 人，允许 1 人或其他不足额组。
- 反馈：选中Soldier脚环为金色，未选为队伍色；底部保留一张“士兵”汇总卡，显示去重后的存活人数、存活者合计生命和指令状态，不按CohortId拆卡。本期未扩展为按兵种拆分的多张UI卡片。

### 移动指令、自由落位与 OrderFormation

- 触发：已有已确认选择时，按 `1` 或点击移动槽进入一次性移动工具；有尚待确认的选兵意图时，也可用 `1` 准备移动或右键排队。下一次有效世界左键提交移动后回到选取，无效点击继续保持移动；右键提交移动意图后也回到选取，保留 Box/Radius 形态。没有存活选择且没有待确认选兵时不能进入移动工具。
- 新选择依赖：移动绑定右键发生时最新提交的具体选兵请求。首次从空选择选兵后也能立即排队移动；只有成功 ACK 与已追上其 revision 的选择副本都到达，才对新成员发送移动。选兵拒绝或结果为空则取消绑定移动，不能转而操作旧部队；该移动未收到回执前不发送会覆盖其依赖的后续选择。生产路径当前只显示 Pending/Accepted/Rejected 命令反馈，不启动本地单位位置预测。
- 取消：拖拽中 `Esc` 先取消未提交拖拽；没有拖拽时，移动工具中的 `Esc` 只取消移动工具，选取工具中的 `Esc` 才提交清空选择。UI 起手不开始世界选兵；拖入 UI 后松开、失焦和角色退出都会取消未提交拖拽。
- 流程：`IssueMove`创建scratch规划任务，依次处理起点验证、候选投影、槽位分配、路由、预约协调和固定步提交。起点验证后按有效人数`N`计算`N + Clamp(Ceil(N×25%), 8, 32)`个目标合法槽，只投射中心优先且不切同距离壳层的候选前缀；合法槽不足时按缺口或至少64个候选继续扩展。普通路径只要64/4查询预算仍有余量，就在同一世界帧连续推进`Validate→Project→Assign→Route→Reconcile`，不再人为每阶段等待一帧。路径或恢复预约在局部备用槽耗尽时，才把尚未提交的事务升级到完整2263池并稳定重跑一次。Pending期间单位继续旧指令；准备结果仍在下一次30Hz权威步统一提交，不暴露半套状态。
- 选择与指令解耦：重新选择、Clear 或 Esc 不会停止已经接受的旧移动指令。
- 终点：围绕点击点按距离由内向外消费1800cm世界轴六角格，最大半径450m、理论上限2263；普通命令只投射满足人数和备用量的中心前缀，2263不再是固定工作量。点击中心或固定5×5块不再是合法性门槛；每个槽仍须通过CommanderSoldier NavMesh、XY修正不超过750cm、半径和1600cm间距检查。
- 预约与分配：同批Eligible成员的旧预约先在scratch表释放，未选友军继续作为硬预约，敌军不预约。9000cm软锚只帮助同一Cohort相对集中；同组用匈牙利匹配减少成员行程。失败成员恢复旧预约后若压住暂定槽，冲突成员继续向外重配。
- 路径：优先用实际起点medoid和终点medoid构建共享长路径；失败时沿终点包围盒最长轴稳定拆组，最终降级为单兵路径。单个成员—槽位失败会换更外候选，候选耗尽才拒绝成员。每兵继续保存路径游标和完整`LastValidNavLocation`。
- 阵形：途中依据可走宽度在1–5列间弹性收窄；收窄立即生效，扩宽需连续15个成功步骤且至少间隔0.5秒。终局固定恢复5列，不再靠松散目标域自然铺开。
- 碰撞与地表约束：Mass Avoidance负责主要局部避让，手工分离只处理仍在移动且实际重叠的成员。每个30Hz步骤的候选位置必须通过`FindMoveAlongSurface`，单步Z变化超过250cm拒绝；失败停在最后合法Nav点并清零速度。到达、空闲和Blocked单位不接受推动，但继续作为静态障碍。
- 到达：距自由槽不超过`max(100cm,速度×固定步长)`时验证最后一段并吸附到合法Nav结果，随后速度/力归零、切换Stand并清除该兵`ActiveOrderId`。选择摘要忽略已完成或Blocked成员的零OrderId。
- 恢复：无进展或表面移动失败时按`Normal → CenterlineRecovery → PersonalPathRecovery → Blocked`推进。个人路径按SoldierId排序，每步最多4次查询；Blocked单位稳定停在最后合法点、退出命令并保留失败上下文，不再抽搐或拖住其他成员。
- 部分接受：同一Cohort可由单兵独立成功。成功成员共享新`BatchOrderId`；失败成员保留旧指令并从当前选择移除。整次命令最多推进一次`SelectionRevision`，Unauthorized、Stale或无导航系统等整单错误不改选择。
- NavMesh重建：导航生成回调只创建分帧修复任务并冻结受影响活动成员；World Tick继续按64次投影/4次规划路径预算修复，下一固定步提交。该路径已实现，但500活动单位重建性能尚未实测。
- 线程边界：Landscape高度、NavMesh投射、预约表和Mass提交继续在Game Thread；纯候选排序及每组最多25人的匈牙利匹配当前同步执行并带Insights范围标记。后续只有实测显示GT峰值超预算时，才评估`BatchProjectPoints`或官方`FindPathAsync`；不把`UWorld`/NavData查询直接丢到任意线程池。
- 反馈：Pending保留橙色，全失败保留短暂红色；Accepted使用逐兵1px静态绿色终点线，不再播放流水箭头。

### 双端权威与即时表现反馈

- 服务端以 30Hz 模拟每名 Soldier，是位置、速度、朝向、指令、碰撞、生命和伤亡的唯一权威。
- 服务端目标以10Hz捕获逐兵PoseChunk，再把一帧的块分摊到三个模拟步发送；10Hz是捕获目标，不保证网络包按固定间隔到达。可靠Soldier FastArray同步身份、队伍、UnitTypeId、float生命、float最大生命和ActiveOrderId，Commander协议版本为v6，旧v5客户端不能混接。权威快照在比较/复制前执行Sanitize；SnapshotRevision使用RepNotify、跨MatchEpoch单调推进，只在事实快照或MatchEpoch变化时向HUD、地图和血条发出一次批量通知。
- v6 ACK按发令时冻结的成员顺序表达Eligible/Accepted掩码；OwnerOnly终点FastArray按SoldierId、`ActiveOrderId`和版本收敛。ACK与终点没有可靠先后顺序，客户端等待选择Revision并按版本配对。网络自动化19/19通过，Standalone双客户端仍未实测。
- 客户端默认在服务器时间后 100ms 渲染，用带速度的 Hermite 插值位置、最短弧插值 Yaw；缺帧最多外推 100ms。
- 点击移动立即显示 Pending 目标反馈，但生产路径当前不再提前移动客户端单位；单位位置始终跟随权威 Pose 流。代码仍保留最长250ms、默认最多900cm及150ms收敛的诊断实现，但2026-09-01已移除生产调用，因为2026-08-31真实鼠标A/B确认旧预测在快速回收和连续改令清零时会造成额外回退。它不能用于掩盖当前约40世界帧的权威规划等待。
- Teleport 或误差超过 10m 才允许硬校正。客户端点选可以据显示姿态提出种子提示，但预测不承担选择、碰撞、命中或伤亡的权威裁决。
- 无动画：FourFRobot 使用静态代理，但移动时持续更新位置、Yaw 和地面贴合。

### 指挥官 UI 与战场反馈

- 相机：WASD/屏幕边缘平移、Q/E旋转、滚轮缩放；共享Landscape子系统提供严格高度与真实边界，相机、HUD和小地图不再各用`±380000cm`常量。SpringArm碰撞收缩和内置Lag关闭，显式求解器以最多1/60秒子步沿摇臂采样净空、限制升降速度并约束完整视锥；`JumpToWorldLocation`复用同一求解。边缘滚屏为16个DPI逻辑像素的SmoothStep，仅在视口聚焦且鼠标位于视口内生效。
- HUD：运行时使用顶中战况条、左下战术地图和底部指挥坞三岛布局；WBP 只负责布局，全部数据和输入逻辑位于原生 C++。
- 指针：本地 Commander 使用约 32 逻辑像素的青白像素箭头、深色描边与尖端热点；由 UE 软件光标直接定位，不额外添加位置缓动。切出 Commander 恢复之前的光标。
- 顶栏：显示真实比赛时长与蓝/红队；当前没有权威来源的目标和比分显示“目标未启用”与 `-- : --`。
- 底坞：保留本地队伍、角色、“在线／同步中”和己方存活数/初始总数；“编队”“指令矩阵”使用中文斜切底板标题，不再使用双斜杠分隔的英文标题。战术点和轨道能量显示 `--`，未实现命令保持禁用。
- 快捷提示栏：底坞上方增加约 56 逻辑像素高的无标题图标栏，当前放置 Alt 同兵种扩选、Shift 追加两个说明条目。图标始终亮；悬停显示用途和组合键，点击只消费 UI 输入，不启动模式、不触发选择，也不穿透世界。条目结构可继续扩展；提示由 viewport 内的原生布局显示。
- 士兵卡：选中部队汇总为一张横向卡；按SoldierId去重后，只统计可靠状态中仍存活的士兵，多Cohort不拆卡。人数显示存活人数，血条按“存活者当前生命总和÷存活者最大生命总和”填充，合计值保留小数；阵亡者退出血量分子与分母。例如25名满血士兵阵亡5名后显示20人，余下士兵仍满血时血条保持100%。
- 卡片状态：没有选择或已确认没有存活成员时折叠；已收到选择但可靠状态尚未就绪、缺少成员状态或未完成本局同步时显示“同步中”，人数与生命显示 `--`，不展示假满血。卡片继续只读，并由选择、可靠状态和命令回执事件刷新；换局清除旧选择，避免复用跨局 SoldierId。
- 小地图：左下角以 10Hz heading-up 批量绘制 Landscape、双方存活兵、选中强调和相机视野框；左键点击地图消费输入并聚焦对应世界位置。
- 选区：Box 拖拽时绘制屏幕矩形；仅 Radius 选取工具显示当前 80m、200m 或 450m 的世界选区圆。移动工具通过既有命令反馈线显示 Pending、Accepted 或 Rejected。
- Pending继续使用橙色反馈，全失败继续短暂显示红色；Accepted以逐兵“权威提交起点→实际最终槽”的1px静态绿色直线显示。绿线没有流动、箭头、脉冲、标签或淡入淡出，只对仍选中、存活且执行对应指令的单位显示；到达、Blocked、死亡、改令、取消选择或网络重置立即隐藏。显示策略与屏幕裁剪自动化已通过，500线运行性能尚未采样。
- 单位脚环：未选为队伍色，选中为金色；保留常显和穿透场景遮挡的行为。Soldier 摧毁后模型隐藏，暗红脚环保留 5 秒后隐藏。
- 世界血条：双方存活 Soldier 在“本地选中或受伤”且距相机焦点不超过 300m 时显示；一个本地 ISM 批量绘制约 84×12px 的黑底、左到右红色连续血条、四分位线和选中青框，接受地形深度遮挡。
- 输入保护：顶栏、小地图、底坞及新增快捷提示栏使用实际 Slate 像素几何阻止世界 LMB/RMB；三画幅、DPI 和完整真实输入回归仍按本轮验收记录判定。
- 角色边界：只有本地 Commander 启用指挥 HUD、相机和键鼠工具入口；`7`、`+`、`1`、Esc、左右键、滚轮以及 HUD/小地图点击都先检查当前角色。切成 Ground/Air 后迟到的旧 UI 点击不能再控制指挥相机。Controller 绑定不吞掉其他 Pawn 的输入；公共工具辅助函数仍可用于内部重置。

### 表现层性能与本地调参

- Unit 与 Ring 继续使用两个稳定 `UInstancedStaticMeshComponent`；始终保持 `SoldierId → InstanceIndex`，不按距离增删实例，也不使用 HISM。
- Unit 以每个实例到当前客户端摄像机的距离计算裁切，默认在 `100000cm` 硬切；Ring 不做距离裁切，因此远距、选中和残骸状态仍可通过脚环读取。
- Unit 默认关闭阴影、距离场照明、动态间接光和光追可见性；Ring 固定关闭同一组重型渲染功能，但继续正常 Raster 显示。
- Ring 网格改为 64 段、128 三角形的单面中空圆环，模型外/内半径为 48/34cm；保留实例缩放 12，世界外/内半径为 576/408cm。材质精简为 6 节点，保留 RGBA 色值、原有发光 `RGB × 3` 和透明度 `A × 0.58`；不改变队伍色、选中色、穿透、无限距和死亡保留时长。
- 正常 Soldier 模型为 `SM_CommanderFourFRobot_Crowd`：单材质、单 Section，LOD tris 为 `18999 / 964 / 238`；旧 FourFRobot 只保留为烘焙源和回退。
- 非 Shipping 构建可用 `gs.Commander.Presentation.List/Get/Set/Reset` 修改当前客户端 World 的六个表现白名单 key；Override 不复制、不写 Config 或磁盘，World 销毁后清空。
- 表现调参优先级为 C++ 默认值 `<` Config `<` 当前 World 本地 Override；非法值拒绝，不静默 Clamp。

### Soldier 数据与运行时 GM

- `GuLiStrikeCommander.xlsx` 的Soldiers表包含A（ID1/100生命）和B（ID2/300.5生命）。Skills定义扫射及同执行器测试变体；UnitSkills按兵种+技能+槽保存伤害、次/秒、厘米射程与默认装备。旧Soldiers攻击力/射程列已移除。
- 每个Game/PIE World初始化时加载和校验，不逐兵逐Tick查表。兵种解析保留安全回退；技能目录出现未知兵种/技能、重复默认槽、无效标签或参数时禁用并报告错误，不偷偷选择其中一行。
- 服务器非Shipping使用`gs.GM.Skill`查看最终配置/来源、覆盖数值、添加/更新/撤销中性来源、替换普攻、查询目标/冷却/停火原因及生成调试兵。Set覆盖位于正常结果之后，Reset只撤该槽数值覆盖，不删除其他来源。
- 导航诊断使用`gs.GM.Commander.Nav.Soldier <SoldierId>`查询目标槽、游标、冷却、恢复状态和失败上下文，使用`gs.GM.Commander.Nav.Stats`查看活动、到达、恢复、Blocked、Surface调用/失败及路径查询聚合；`gs.GM.Commander.Camera.Debug <0|1>`独立控制相机调试。以上命令仅存在于非Shipping构建。
- `gs.GM.Commander.Nav.LastMove [CohortId]`输出最近请求目标、候选、掩码、冲突、拆分、查询和规划耗时；`Nav.Stats`增加Pending规划、候选投影、路径查询、单兵部分接受和结构化失败阶段。候选失败只计数，最终命令才输出一次汇总Warning。
- 原`gs.GM`保留生命、移动等现有调参；最大生命按原生命比例换算，死亡单位保持0。旧攻击力/射程键明确提示改用`gs.GM.Skill`。两套调试入口均不写回Excel、DataTable、ini。

### 扫射与指挥官 GAS 桥接

- 停止、移动和导航恢复期间都自动攻击射程内敌方Mass小兵，不追击；移动状态不清目标、不刷新冷却。每200ms分批索敌、每次开火复核，目标失效后选最近者，同距按SoldierId。
- 两兵种共用扫射执行器，A为10伤害/2次每秒/100m，B为7.5伤害/4次每秒/150m。独立冷却，换目标不刷新，无补射积累；攻速变化保留冷却比例，主体替换等完整新冷却。同一步先收集再结算，允许互杀。
- 公共BattlePlayerState增加ASC，仅Commander获得军队能力。ServerOnly GA向World/Team账本提交来源；Mass只读已解析缓存，不为兵种或单兵新建ASC。客户端只接收最终配置和权威战斗结果，不逐发RPC、不结算伤害。
- 来源ID更新不重复叠加；两个独立+20%为×1.44。技能优先级替换和数值修改整笔校验，冲突保留旧状态。来源随团队保留，重生/指挥官换人不靠旧ASC自动撤销，新战局清空。
- 执行器注册支持新行为，附加效果仅留接口；没有科技树、卡牌、火炮实体、燃烧或穿透。详细规则见[战斗](战斗.md#小兵普通攻击扫射)，GM示例及本期验证见[开发记录](../DevelopmentDocumentation/20260901-小兵扫射与指挥官GAS桥接.md)。

### 本轮验证边界（相机、编队导航与移动射击）

本轮完整Game Development、Development Editor与DebugGame Editor构建成功；DebugGame导航20/20、全量`GuLiStrike`89/89及当前Development导航20/20通过。Launcher版UE5.7不支持Server target，Dedicated Server构建仍为环境限制。旧`BP_GuLiStrikeGameMode`已从LFS恢复但仍缺三个TwinStick依赖；默认GameMode已切到原生`GuLiCommanderGameMode`。当前PIE的0/500已归因于出生中心Z=0、旧投影垂直范围5000cm无法覆盖约Z=-7298至-7058的Nav面；旧DLL临时使用Z=-7000可生成500兵。正式把出生专用垂直范围改为50000cm后，Live Coding PIE记录`Spawned 500`且GM为`alive=500 idle=500 blocked=0`；关闭旧编辑器、完整构建并冷启动新进程后，04:55:52再次记录`Spawned 500`，证明并非仅Live Coding补丁生效。当前PIE保持运行；移动、`OrderId=19`、相机、双客户端和性能仍待验证。完整边界见[本轮归档](../Archive/20260901-指挥官选兵移动导航与小兵GAS扫射总归档-0831至0901.md)。

### 当前验证边界（自由落位、v6与静态绿线）

[移动命令自由扩散与静态寻路线](../DevelopmentDocumentation/20260901-移动命令自由扩散与静态寻路线.md)的源码、v6掩码、OwnerOnly终点与无动画静态绿线已经实现。Editor/Game Development构建成功；全量99项均Success、网络19/19、导航24/24。默认地图无头运行加载原生Commander GameMode、生成500兵并完成20/20成员的一次规划、3870cm移动与销毁链，最终PASS。默认Game/Editor地图已切到`LVL_CommanderMassPrototype`，避免`LVL_Main`、`LVL_ShipTest`仍绑定旧Ship GameMode及缺导航数据造成0兵。25/250/500交互PIE、双客户端、500线绘制性能、500活动单位NavMesh重建压力和Dedicated Server仍未验证，完整证据见[受限验证归档](../Archive/20260901-指挥官选兵移动导航与小兵GAS扫射总归档-0831至0901.md)。

### 历史验证边界（精确选兵与快捷提示栏）

本轮Editor Development最终冷编译通过，全GuLiStrike 61项自动化通过（60项Commander，2项预期fallback警告）。21个有效Slate鼠标选兵检查、额外Windows失焦取消、双客户端己方单选与约532ms RTT下待确认选择→移动、三画幅HUD/Alt与Shift提示验证均有证据。默认移动算法不变；正常网及弱网A/B确认额外回退，6条连续弱网命令的迟到ACK已完整去重记录。

物理DPI切换、角色退出光标还原、打包游戏Escape、真实连续选择/移动的所有极限网络组合尚未逐项实测；不据编译或合成测试推断已通过。详情见[实施与验证](../DevelopmentDocumentation/20260831-指挥官精确选兵与快捷提示栏.md)、[移动诊断](../DevelopmentDocumentation/20260831-小兵客户端先行移动拖拽诊断.md)及[本轮归档](../Archive/20260901-指挥官选兵移动导航与小兵GAS扫射总归档-0831至0901.md)。下方保留的公共 Battle 框架验证属于历史基线，不能替代本轮精确选兵和输入验收。

### 历史验证基线（公共 Battle 框架接入）

撤回额外补 ACK 重写后，最终冷编译耗时 14.17 秒；全量 GuLiStrike 自动化测试 50/50 成功，exit 0。Listen Server 指挥官身份、名册及同 GUID/席位重生已通过。

原客户端使用 open 重新连接后 GUID、席位不变。Dedicated Server + 2 个独立 -game 客户端通过原生 Timer 调用 RestartGame，同图无缝重建后 MatchId 更新、原 GUID 与槽位 0/5 保留，两道就绪门均通过。Listen Server + 1 客户端通过原生 DEFER servertravel 完成“原型图→ShipTest→原型图”往返，Controller 为 Native→BP_PC_Ship→Native，身份不变；ShipTest 无导航时名册未就绪、公共就绪为 true，回原型后两门均为 true。

混合角色测试最终为 10 名玩法玩家（2 Commander、4 Ground、4 Air）和 2 名观察者，12 个 GUID 唯一；全员 BattleReady、SoldierReady，4 艘 Air 飞船均 ShipReady。两名观察者分别来自出生碰撞和真正满席；只在运行时分开占据出生点的船体，未保存地图。

跨角色验证中，Ground 输入注入移动、同 GUID/席位复活及 EndMatch 取消排队复活通过。Ship 原生 Timer 弱网复活、NativeTick RPC 换装/服务器开火及原生失占有停火通过；范围和采样分别见[战斗验证边界](战斗.md#本轮验证边界)与[飞船验证边界](飞船.md#本轮验证边界)。

NetworkGate 尚未整体 PASS：旧三轮分别为后台节流导致无效、ACK 指标 159.9ms、ACK 指标 146.4ms 但 snaps=3。第 4 轮有效 DEFER 复测为 ACK P95=138.1ms、入站平均/P95=0.459/0.467Mbps、gap=384.9ms、硬跳变 1 次，仍保留失败结论。

此前直接在 Python/Slate 中触发的同步切图、占有 RPC 受 FEditorScriptExecutionGuard 强制本地执行；受影响的旧失败和重占有结论均不作为原生联机证据，也不归因于 UE 缺陷。补 ACK 重写已撤回。完整实体内容、FullWASD 实际按键与主观画面体验仍未全面覆盖。

上述公共 Battle 框架接入的完整记录见[正式归档](../Archive/20260831-公共战局框架与三类角色接入.md)。

## 数值速查

| 数值 | 值 | 备注 |
|---|---:|---|
| 原型 Soldier 总数 | 500 | 红蓝各 250 |
| ControlCohort 容量 | 最多 25 人 | 内部临时分组；可精确单选 1 人，不是最小选择人数 |
| 默认选择形态 / 拖拽阈值 | Box / 6 逻辑像素 | 松开提交；按 viewport DPI 换算，`7` 切换 Box/Radius |
| 选择半径 | 80m / 200m / 450m | 仅选取工具的 Radius 形态通过键盘 `Add` 或 `Shift+Equals` 循环，无 `+` 按钮 |
| 自动补员 | 禁用 | 旧圆外 300m 补齐 → 全部选择只保留实际命中成员 |
| Alt 同兵种本次候选上限 | 1000 人 | 全图按二维距离/ID 排序；不限制 Shift 累计人数 |
| 普通/累计选择协议上限 | 10000 人 | 400 组 × 最多 25 人；追加时保留已有成员 |
| 服务端模拟 | 30Hz | 固定步 |
| 相机子步 / 每帧上限 | 1/60s / 8步 | 摇臂地形采样间隔不超过10000cm |
| 相机净空 | 枢轴150 / 摇臂200 / 镜头500cm | 上升/下降限速30000/15000cm/s，边界余量5000cm |
| 边缘滚屏 | 16 DPI逻辑像素 | SmoothStep；仅视口聚焦且鼠标在视口内 |
| Soldier 移速 | 36m/s | `DefaultSoldier` DataTable 基线，可由 World GM Override 临时覆盖 |
| Soldier 转向 | 90°/s | 当前原型值 |
| Commander AgentRadius | 7.5m | 服务端 Mass/Nav Agent |
| 红/蓝出生中心 | `(-10000,127500)` / `(110000,37500)`cm | 平面距离150000cm；朝敌方中心；500槽创建前原子预检 |
| 出生Nav投影范围 | Z 50000cm / XY纠正上限750cm | 垂直范围仅供出生投影；不放宽横向修正、唯一性和1600cm间距 |
| 自由落位 | 六角格距1800cm / 最大450m / 理论上限2263 | 普通命令按`N+25%`有界备用量投射中心前缀；XY修正≤750cm；槽及硬预约间距≥1600cm |
| 规划/重建修复帧预算 | 64次候选投影 / 4次规划路径查询 | 正常阶段同帧前向推进；DebugGame真实20兵样本为31个落点、51次总投射、1路径、1世界帧、16.582ms；500活动单位重建性能待测 |
| 成功寻路线 | 绿色1px静态直线 | 无动效；权威起点到实际终点；生命周期与ActiveOrderId绑定；500线性能待测 |
| 到达吸附阈值 | `max(100cm,速度×固定步长)` | 默认36m/s、30Hz时为120cm；最后一段仍需Nav验证 |
| 途中阵形 | 1–5列 | 收窄立即；扩宽连续15个成功步且至少间隔0.5s；终局5列 |
| 表面移动Z阈值 | 250cm/固定步 | 每步`FindMoveAlongSurface`；超过则停在最后合法点 |
| 恢复阈值 | 2次失败或1s无进展→中心线；再1s或累计6次→个人路径 | 个人路径每步最多4次；2s重寻一次，再2s进入Blocked |
| Pose 捕获 | 10Hz | 每 3 个服务端固定步 |
| 插值回看 | 100ms | 客户端表现时间 |
| 最大外推 | 100ms | 之后保持 |
| 硬校正阈值 | 10m | 或显式 Teleport |
| 表现预测 | 生产路径停用 | 诊断实现仍保留250ms / 默认9m；当前单位位置只跟随权威Pose |
| 预测收敛 | 150ms（诊断实现） | 生产路径不启动旧预测；若未来重做需重新验收交接与连续改令 |
| 残骸脚环 | 5s | 模型摧毁后隐藏，仅脚环保留 |
| Unit 兵模硬切 | 1000m | ISM Start/End Cull 都为 100000cm；每客户端独立 |
| Ring 脚环硬切 | 禁用 | Start/End Cull 都为 0，远距继续显示 |
| Ring 网格 | 64 段 / 128 三角形 | 单面中空；模型外/内半径 48/34cm |
| Ring 世界外/内半径 | 576/408cm | 保留实例缩放 12 |
| Ring 材质 | 6 节点 | RGBA；发光 `RGB × 3`，透明度 `A × 0.58`，保留穿透 |
| Crowd LOD tris | 18,999 / 964 / 238 | 单材质、单 Section；ScreenSize 1 / 0.056 / 0.028 |
| Crowd 贴图 | 3×2048² | BaseColor、Normal、ORM |
| Commander 当前协议 | v6 | float生命；cohort成员Eligible/Accepted掩码与OwnerOnly活动终点；旧v5拒绝；网络自动化19/19，双客户端待测 |
| 当前 Soldier UnitTypeId | 1 | 仅增加稳定兵种标识，不新增实际兵种 |
| 软件指针 / 快捷提示栏高度 | 32 / 约 56 逻辑像素 | 尖端热点；Alt/Shift 图标常亮、仅说明 |
| HUD 计时刷新 | 1Hz | 其余事实主要由原生委托驱动 |
| 底部士兵卡 | 当前兵种 1 张横向卡 | 选中 ID 去重；只显示存活人数及活者合计当前/最大生命 |
| 战术地图刷新 | 10Hz | 单原生绘制层 |
| 世界血条 | 约 84×12px，焦点 300m | 一个本地 ISM；仅选中或受伤的存活 Soldier |
| 局部流场 | 默认关闭 | 500cm 单元、64×64；待专项验收 |

## 演进记录

- 2026-08-27：初版 500 人 Commander Mass 原型形成，随后修复 HISM/周期性重建造成的频闪和场景过暗。
- 2026-08-27：澄清“25 人一组”为动态最小控制粒度；永久 CommandGroup 迁移为 SoldierId、临时 ControlCohort 和 OrderFormation。
- 2026-08-27：由 5Hz 组摘要直驱表现迁移为 10Hz 逐兵 Pose、Hermite 插值、外推和短时预测；保持服务端权威。
- 2026-08-27：接入 CommanderSoldier 共享 NavMesh、弹性 1–5 列、5×5 终点足迹、Hungarian 槽位与 Mass Avoidance。
- 2026-08-27：接入可靠 Bootstrap/FastArray、Fast Intent + Reliable fallback、受损网络 Gate、稳定 ISM、脚环、小地图、动态 Cohort 卡和流水命令线。
- 2026-08-27：功能与受损网络平滑基线归档；性能、复杂导航、500 人同时移动、seamless travel 和流场默认开启仍待专项验收。
- 2026-08-28：`DefaultSoldier` 改为 Excel/DataTable 驱动，基线移速 18m/s → 36m/s；模型、生命和战斗 Stats 使用同一运行时定义。
- 2026-08-28：远距 Landscape 射线扩展到 600000cm；移动改为同批共享目标与松散到达，不再验证/恢复终点 5×5 方阵。
- 2026-08-28：小地图内容改为 heading-up；新增 7 个 World 会话级 `gs.GM.*` 调参 key，并同步 Soldier 预测与飞船 `GM.Runtime` 修饰层。
- 2026-08-29：Unit ISM 加入 100000cm 硬切、Ring 保持无限距；新增六键本地表现调参，并用单材质/单 Section/三 LOD Crowd 网格替换正常运行时英雄级 FourFRobot。
- 2026-08-29：三岛式 UMG HUD 接入现有 Commander 运行时；输入改为默认 Select、`1` 一次性 Move、`7` 返回 Select、`+` 循环选取半径；可靠 Soldier 状态升级为协议 v3 Health/MaxHealth，并新增单 ISM 世界血条。
- 2026-08-31：底坞标题改为中文斜切底板；当前兵种的多组选择合并为一张横向“士兵”卡，仅统计去重后的存活人数及活者合计生命，阵亡退出血量分子分母。移除 `+` 按钮，保留两种键盘加号输入；底层 ControlCohort、RPC 和协议 v3 不变。
- 2026-08-31：脚环改为 64 段、128 三角形单面中空网格与 6 节点材质；保留世界半径、颜色、发光/透明度、常显穿透及死亡暗红 5 秒行为。

- 2026-08-31：公共 Battle 基类统一角色分配与 Pawn 重生，原 Commander 类保留兼容；士兵世界调度提取为 `UGuLiCommanderWorldReplicationComponent`，连接区分 BattleReady 与 SoldierStreamReady，专业协议仍为 v3。指挥键鼠/UI 入口按角色启用，不改变现有选兵、寻路或反馈算法。
- 2026-08-31：精确选兵代码将默认圆形选取改为 Box 点选/拖框，6 逻辑像素区分点击与拖拽，`7` 从幂等返回 Select 改为 Box/Radius 往返。全部形态取消补满 25 人；Shift 从整组 Toggle 改为成员并集，Alt 按同类型全图距离序选最近最多 1000 人。移动改为依赖具体最新选择的成功 ACK 与选择副本，协议 v3 → v4 并加入 UnitTypeId；新增软件箭头与常亮只读 Alt/Shift 提示栏。本项运行验收不沿用上述历史基线。
- 2026-09-01：多兵种配置接入通用扫射和World团队来源桥接；公共ASC授予服务器军队能力，GM沿真实GA入口验证。生命/伤害全链float，协议v5；科技树与卡牌暂不接入。
- 2026-09-01：相机改用真实Landscape边界与显式净空求解；红蓝部署缩短至1500m；移动恢复固定5×5终点、每步NavMesh表面约束与Blocked退出，扫射允许移动中开火。Game/两种Editor构建、DebugGame全量89/89及Development导航20/20已通过；GUI Editor已加载当前DLL，交互、双客户端和性能仍待验证。
- 2026-09-01：修复`LVL_CommanderMassPrototype`的0/500出生。出生专用Nav投影Z范围5000→50000cm，XY纠正仍为750cm；Live Coding PIE生成500兵，3分钟后GM为`alive=500 idle=500 blocked=0`，完整Development构建后的冷启动PIE于04:55:52再次生成500兵。移动性能与双客户端未因此判定通过。
- 2026-09-01：取消固定5×5终点硬门槛，完成450m内六角候选自由扩散、单兵部分接受、分帧规划、NavMesh分帧修复和协议v6；Accepted反馈改为1px静态绿线且不带动效。两种Development构建、全量99项、网络19/19、导航24/24及默认地图20兵无头命令链通过；交互PIE、双客户端与性能仍待验证。
- 2026-09-01：后续只读诊断确认移动启动延迟的主因：单兵同样完整投射2263个候选，每世界帧预算64次，最短约40世界帧；严格单兵3 FPS样本为2264次总投射、1次路径、13.002秒，与模型吻合。10Hz Pose和100ms插值只构成次要可见叠加。生产单位位置预测已停用，旧250ms实现仅保留诊断代码；本轮未修改移动算法。
- 2026-09-01：实施移动首动延迟修复：保留2263稳定候选编号与每组≤25人的匈牙利分配，把NavMesh投射改为`N+有界备用`的完整距离壳层前缀，非法时确定性扩展，路径/预约备用耗尽才升级完整池；正常阶段同一世界帧前向推进。Win64 DebugGame构建成功，DestinationPlanner 10/10、导航26/26、全量101项0失败；默认地图真实20兵样本为`projected=31`、`frames=1`、`paths=1`、`planningMs=16.582`并移动3970cm通过。
