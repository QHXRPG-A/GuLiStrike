---
schema: guli-progress/v1
id: DEV-20260923-003
work_id: WORK-20260923-003
kind: development
role: root
title: Mass分帧寻路与避障稳定性
areas: [commander, network, performance]
categories: [gameplay, performance]
status: verification
verification: partial
created: '2026-09-23'
updated: '2026-09-24'
summary: 同址PIE复现定位赠品导航范围缓存错误与恢复出口误拒绝；R2已编译加载，90秒采样59名成员全部离开现场，10Hz保持。
next_action: 玩家在LVL_CommanderMassPrototype的MassNavRepair_GiftEdge确认R2效果。
relations:
  requirement: REQ-20260923-003
status_note: 原即时响应和随机站位验收继续有效；第一版收到同址卡死反馈，R2静态检查、场景保存、编译加载及用户要求的同址PIE复测完成，待玩家确认；未扩大到完整障碍矩阵。
---

# Mass分帧寻路与避障稳定性

## 当前实现

保留工作区既有修改，在最初分帧规划版本上改为“命令立即生效＋后台共享NavMesh路线”。没有增加流场烘焙、积分网格或流场资产。最初匈牙利匹配、预约、连接复核和失败拆组的实现说明不再作为当前行为；历史见下方归档。

`UGuLiUnitTaskSubsystem::Submit`为整条命令创建原生`FGuLiSharedMoveIntent`，在已有接纳循环冻结成员。该上下文只存于原生命令成员，不反射、不序列化；Active、PendingMove、Queue和各规划子批共享持有，取消/完成后释放。世界只保存弱引用并按游标清理。所有接纳片均使用完整命令的成员和最大模型宽度，避免每8/25人重新分配一套相同站位。

`PumpMoveAdmissions`只唤醒待处理成员，每帧最多128人，每片最多8人。`BeginMovePlanning`、原生句柄、增量进度及`PollMovePlanning`兼容保留；现在完成表示意图已经生效，不表示路线或站位已完成。每个<=25人的批次复核任务版本、控制权、存活/运输、战局后立即提交，不等10Hz。起点取实际生效位置；原生名册增量同帧发布。

`FGuLiSharedMoveRoutePool`按点击目标、NavData及起点多边形共享请求，并复用已有走廊覆盖的多边形。保存原生路径/走廊/门户及索引，禁用引擎自动重寻路。允许部分路径；没有方向则直接趋近，保留NavMesh表面和环境约束。路径失效只进入相关依赖复核；查询仍在统一预算，负结果按代次懒失效。空闲路线30秒淘汰，废弃排队项尽快释放，派生路线缓存上限128MiB。重试创建新路线实例，旧走廊索引随旧实例退休，防止旧覆盖索引指向新走廊。

站位由`FGuLiSharedMoveIntent`分帧处理：先按兵种缓存`GetModelBoundsCentimeters()`的缩放后XY最大边，归约整个命令的最大值，再生成中心及6n个点的同心圈。每个有NavMesh覆盖的点，从未分配成员中随机抽取一名，用swap/pop移除，存入`SoldierId→站位`索引。没有距离优化、占用查询、配对搜索或全局预约。命令/停止的版本约束继续生效，旧意图的点不会写入新意图。

圈距等于宽度W，同圈相邻直线距离为`2*r*W*sin(pi/(6*r)) >= W`；各圈之间任意两点距离也不小于W。只对原XY进行竖向NavMesh覆盖查询，最远25W；若点数不足，不重用别人的点，未分配成员保留移动意图。接近已生成停靠区域后从共享长途走廊切换到自身固定站位的直接趋近和现有避障，不为每个站位创建完整路径请求。停靠目标不再被1秒无进展或“最近点”逻辑改写。末段期望速度按剩余距离/实际步长限幅，20cm到达容差，不直接吸附。移动失败仍保持最后合法位置，成功以真实位移回写速度。

绿线仍是实际生效起点到点击点。v19、持久批量线组件、网络ACK独立于绘制队列、128条/0.5ms处理、1000字节/批和4批在途保持；不额外复制逐兵站位。

## 预算与静态检查

| 项目 | 当前结果 |
|---|---|
| 规划 | 2ms/世界帧，64次位置/依赖检查、4次共享路径；本次改为手动1.0ms、自动0.4ms、站位及维护0.3ms、脚下恢复0.3ms，位置额度16/16/24/8，0.2ms短片和额度借用 |
| 提交 | 独立0.5ms、100人，原子子批<=25；下一世界帧接纳，位移仍10Hz |
| 旧算法旁路 | 手动及自动使用新意图；新意图的移动失败不进入个人恢复；不调用旧目标匹配、连接、拆组、预约、lane/slot或局部流场工作入口。旧辅助实现保留但不接入新流程 |
| 生命周期 | 任务持有整单上下文，世界弱引用；命令版本/死亡/控制/战局检查保留；新路线重试不复用旧覆盖索引 |
| 间距 | 读取最终PresentationScale后的模型包围盒；抽签仅删除未分配成员，保证同单唯一；所有子批共享一个上下文 |
| 接口与类型 | UHT、C++编译和链接通过；`git diff --check`通过，没有新增自动化测试 |
| 同步 | 原生状态/点击目标同帧增量；协议格式及客户端插值保持；不把分配目标当作实际位置同步 |

单次引擎查询与原子提交不能中途抢占；预算不是绝对墙钟上界。移动表面约束和10Hz模拟仍按原功能运行。

## 编译与动态数据

**本节属于本次导航恢复补丁之前的即时响应/随机站位验收，不证明新补丁已经编译或运行。** 新补丁状态见文末。

沿用用户明确授权，源码引擎`D:\UnrealEngine-5.7`，目标`GuLiStrikeEditor Win64 Development -WaitMutex -NoHotReloadFromIDE`。本轮最终构建4退出0，耗时47.10秒；引擎、项目和6个原生插件的BuildId均为`dd3ee083-a0fd-45c8-814e-67fe5ef95e31`。编辑器PID55800来自源码引擎，实际加载项目最新两个DLL，时间晚于构建完成。没有运行自动化测试、Live Coding或打包。

原型图默认500名Mass，自动推进开启，同进程专用服务端+两个客户端；通过蓝方客户端正常有序命令入口采样约134秒。以下全部是客户端输入到事件的进程内时间；首次绿线仅指提交渲染，不是屏幕实际呈现：

| 样本 | 首个/全部首次真实位移 | 首条绿线提交渲染 | 分配结果 |
|---|---|---|---|
| 970104：100名扫荡者 | 37.63 / 37.98ms | 94.71ms | 100个不同点，最小点距302.1047cm，整单宽度302.1048cm |
| 970108：50名混合兵种 | 90.47 / 90.72ms | 122.01ms | 50个不同点，最小点距1249.9036cm，整单宽度1249.9041cm |
| 970109：混合50人改令 | 103.64 / 103.96ms | 135.59ms | 0.18秒后被替换，采样未捕获全部旧站位，不据此判断分配失败 |
| 970110：再次改令 | 89.40 / 89.62ms | 117.04ms | 50个不同点，采样中目标没有被最近点逻辑改写 |

模型直接读取尺寸：扫荡者XY约302.1048×261.1492cm；战争机器约1241.4559×1249.9040cm。混合选择取后者宽度，未改模型尺寸。

100人样本约63秒时98人在20cm范围内，另2人距站位约26/30cm，仍有局部避让压力；混合50人样本约58秒时48人在20cm内，2人受地形/拥堵影响，约2369/107cm未到位。之后人为停止/改令。因此只确认分配唯一和响应，**不宣称全部到达**。此前125人响应采样也曾出现输入到服务端接收约392ms的单帧停顿，最新样本达标不能代表所有场景稳定低于200ms。

229次状态样本最大：位置检查64、路径4、提交100人，规划1.496ms、提交0.191ms；共享路线缓存约4.78MiB。个人恢复和预约条目为0，客户端无无效绿线或端点不匹配。停止后线数/队列均0，重同步代次已递增且ready=true。短采样不能替代长期泄漏及完整弱网回归。

证据：[最终构建](../../Artifacts/MassImmediate20260924/compile-4.log)、[8份BuildId](../../Artifacts/MassImmediate20260924/build-4-ids.json)、[实际模块加载](../../Artifacts/MassImmediate20260924/loaded-build-4.json)、[模型尺寸](../../Artifacts/MassImmediate20260924/model-bounds.json)、[动态汇总](../../Artifacts/MassImmediate20260924/runtime-slots-summary.json)、[原始采样](../../Artifacts/MassImmediate20260924/runtime-slots.jsonl)、[原始日志](../../Artifacts/MassImmediate20260924/editor-3.log)。

## 场景交付与玩家操作

地图`/Game/Maps/LVL_CommanderMassPrototype`，默认红蓝各250名军队及原资源配置，无DeploymentPoint覆盖。Outliner文件夹`GuLiStrike/Review/MassNavigation`，14个带`MassNavigationReview20260923`标签的对象：8墙体、5说明Note、1相机。已保存新的站位说明并回读实体，119个Actor的原变换保持；PIE成功通过地图前置门禁。

| 区域/Actor | XY(cm) | 操作与预期 |
|---|---|---|
| MassNav_Entry | 0,72500 | 先S停住，选择25/100人或混合兵种，再右键/M+左键；应立即行进、逐步分散至各自随机站位，混合选择按最大模型留距 |
| CornerLong/CornerReturn | 11200,70500 / 12600,71500 | 墙后下令，共享走廊准备后绕行；仍不得穿墙或直接吸附到站位 |
| NarrowLeft/NarrowRight | 5200,69000 / 6200,69000 | 800cm通道；保留表面及环境约束，大单位可能长期受阻 |
| Closed* | 中心16500,67000 | 没有导航覆盖/封闭目标；未到达者保留意图，停止后旧线不可复活 |
| OreMechObserve | 5625,71875 | 矿体消失、机甲通过、近处及远处造楼的相关失效 |
| Overview | -12000,85000；Z28000 | 编辑器观察相机 |

维护脚本[author_mass_immediate_scene.py](../../Scripts/author_mass_immediate_scene.py)只改说明并保存，实体回读见[场景交付](../../Artifacts/MassImmediate20260924/scene-delivery.json)。连续改令、停止、重同步已采样；双指挥官同时手动操作、真实重连、唯一通道变化、500/2000/10000单方规模及完整障碍矩阵尚未全部验证。大人口合法部署入口沿用原待办，不改人口容量规则。

## 历史与验收状态

玩家先确认即时响应延迟较低，并给出拥挤截图；随机唯一站位及包围盒间距修复后，明确反馈“符合预期，收尾并总结”。本次功能置done/passed；该验收不扩大为上文未覆盖的大人口、弱网和完整环境矩阵已通过。

历史：[最初场景PIE修复](../Archive/20260923-Mass验证场景PIE阻塞修复.md)、[原始延迟诊断](../Archive/20260923-Mass首条绿线延迟现场诊断.md)、[旧算法三阶段实测](../Archive/20260924-Mass三阶段耗时与百人响应实测.md)、[本轮交付归档](../Archive/20260924-Mass即时移动与随机站位交付.md)。早期基线位于`Artifacts/MassNavigation20260923`；即时移动前源码基线位于`Artifacts/MassImmediate20260924/baseline`，当前页重写前的文档保留于同目录`before-docs`。

## 玩家验收确认（2026-09-24）

用户反馈：“符合预期，收尾并总结”。本次即时响应与随机唯一站位已获确认，源码版编译和PIE动态证据见上文。采样诊断已关闭，当前PIE保持运行。大人口部署及未覆盖的完整回归继续按各自边界记录。见[验收增量记录](../Archive/20260924-Mass随机站位玩家验收.md)。

## 2026-09-24 第一版：建筑边缘卡住修复，检测10Hz（同址反馈见下方R2）

### 现场原因与实现

用户先要求查明当前PIE所选单位卡住原因，随后明确“修复，检测频率10Hz”。现场同类受阻单位452/630的脚下`nav_ref_valid=0`，原10cm重投影失败；最近导航面分别约27.8/110.5cm。新即时移动分支绕过旧个人恢复，且旧恢复入口没有调度，导致持续SurfaceMoveFailed和零速度。原12名选择成员随后变化，不能将每名成员的最终失败码都视为已经捕获。见[现场诊断](../../TestResults/CurrentPIE/stuck_units_diagnosis.md)。

本次只修复失效脚下坐标：

- `DetectNavigationRepairs`位于`TickAuthority`，每个0.1秒固定步调用一次；扫描所有现存、可行动单位，包括空闲或自动攻击暂停者。编译期锁定共享时钟`RateHz==10`，与渲染帧率及规划配额分开。
- `TickNavigationRepairs`接入第四类预算；每单位每世界帧至多一个候选：原点、300cm范围最近点、八个局部方向。查询和物理检查计入规划耗时；总额保持2ms/64位置/4路径。有限候选都失败后，下一检测步可重试，任务不会因失败或时间超限结束。
- 安全落点要求XY纠正≤300cm、Z差≤50cm；动态障碍允许向外脱离，但落点须完全清空；建筑碰撞检查使用球体重叠及正反向扫掠，初始穿入只能沿外法线退出，不允许跨越沿途墙体。
- `CommitReadyNavigationRepairs`在10Hz位移阶段末尾、战斗采样前提交。复核单位存活/相位/锁定、任务版本、命令、战局、NavData、原位置和目标多边形；使用最新障碍快照再次验碰撞。远处无关tile更新不直接废弃仍有效的局部结果。
- 更新服务端位置、脚下引用、Mass Fragment及已有可靠位移屏障；清理失效共享路线引用后，由原共享路线池续接。保留命令、自动/手动标志、随机站位及绿线起终点，不恢复个人A*、改站位或预约。
- 诊断新增`recovery_detection_hz`、`recovery_detection_steps`、`recovery_queries`、`recovery_repairs`及逐兵`nav_repair_pending`。`recovery_failures`表示一轮候选耗尽，不表示任务终止。

### 静态检查与交付

- [x] 审查C++声明/定义、10Hz调用位置、预算总量、停止/改令/死亡的过期结果处理、位移状态流；对照本机UE5.7的World/HitResult接口。
- [x] `git diff --check`通过；场景脚本Python语法检查通过。未新增自动化测试；[静态核对记录](../../Artifacts/MassNavRepair20260924/static-review.json)不代替C++编译。
- [x] 场景作为实现与静态检查后的最后一步保存，回读12个相关实体（8墙、3说明Note、1相机），总Actor数122；原有Actor变换保持。
- [x] 本次用户选择“编译并加载”：源码版UE5.7的GuLiStrikeEditor Win64 Development构建退出0，用时45.53秒；引擎、项目及6个原生插件BuildId一致。PID69924在构建后启动，实际加载本次两个项目DLL，重新打开原型图并回读3个新增观察实体。
- [ ] 玩家验证：等待加载新代码后的建筑边缘恢复、停止/改令及禁止穿墙效果确认。

### 场景入口与操作

Map：`/Game/Maps/LVL_CommanderMassPrototype`。编辑器原已关闭，本轮重新打开源码版编辑器，仅保存场景，未启动PIE。使用[场景维护脚本](../../Scripts/author_mass_surface_repair_scene.py)；[保存与实体回读](../../Artifacts/MassNavRepair20260924/scene-delivery.json)。

Outliner：`GuLiStrike/Review/MassNavigation/SurfaceRepair`。

| 对象 | 位置cm | 用途 |
|---|---|---|
| MassNavRepair_GiftEdge | 40397.49,41393.72,-1359.91 | 原赠品建筑边缘现场；原建筑中心XY=40000,41400 |
| MassNavRepair_FactoryEdge | -1523.50,-40727.17,-1485.49 | 原工厂边缘现场；原建筑中心XY=-1400,-40000 |
| MassNavRepair_Overview | 44000,45500,6500 | 编辑器观察相机 |

加载本次编译产物后，由玩家进入PIE，使用默认军队及正常B建造/赠品生成流程，在移动路线邻近位置生成建筑。若重建使单位脚下引用失效，应短距离安全退出边缘后继续同一命令，固定站位和绿线端点保持；诊断的检测步数随模拟步推进，检测Hz为10。恢复查询和提交受预算影响，不承诺每次都在0.1秒内完成。

在待恢复时发新命令或S停止，旧恢复不能复活旧任务。沿用`MassNav_Corner*`与`MassNav_Closed*`观察禁止穿墙和不可达时持续保留任务。无合法落点、水平纠正超过300cm或高度超过50cm时应保持等待，不能将此修复理解为所有拥堵/不可达目标都保证到达。

本次增量见[10Hz导航恢复源码/场景归档](../Archive/20260924-Mass导航失效10Hz恢复修复.md)和[编译加载记录](../Archive/20260924-Mass导航恢复编译与加载.md)。编译证据：[构建日志](../../Artifacts/MassNavRepair20260924/compile.log)、[BuildId](../../Artifacts/MassNavRepair20260924/build-ids.json)、[实际模块加载](../../Artifacts/MassNavRepair20260924/loaded-build.json)、[重开地图回读](../../Artifacts/MassNavRepair20260924/reopened-scene.json)。本轮未启动PIE、自动化测试或运行时效果验收。

## 2026-09-24 R2：同址复现、赠品导航范围与出口误拒绝

用户反馈第一版仍卡住，并要求通过接口分析、再次启动PIE复现。旧后台实例已关闭；实际现场PID71460加载的是12:37编译产物，10Hz检测确实运行。重启后赠品中心`(-17900,60000)`附近再次出现停滞，捕获152/157/174和另一轮157/164/165/169/175。约第21秒起位置停止，持续重试仍失败。

根因及最小对照：

- 赠品在禁用碰撞/导航期间调用`ApplyDefinition → UpdateNavigationBounds`，缓存默认100cm范围；提交只恢复开关，未重算真实`400×300×200cm`半尺寸。导航遗留实体内部可走小岛。现场仅刷新范围后，两个内部探测点从可走变为不可走，四名受困单位立即恢复；外部探测点仍可走。
- 第一版300cm候选使用float圆周常量，严格距离平方比较把`90000.00000000001/90000.00000000068`误判超距；157/164/169/175的安全出口均被此门限拒绝。
- 151的清空落点约301.29cm，165在刷新后的水平出口约323cm；旧半径不足。165处于矮箱体内时引擎最小穿透法线朝下，原算法同时拒绝水平退出。

R2实现：恢复真实碰撞/primitive导航标志后先`UpdateNavigationBounds`再恢复导航相关性；恢复角度改双精度、距离容差0.01cm，300/600cm两圈共18候选，最大高差仍50cm。对直立箱体内部的垂直穿透法线使用最近水平面，保留向外约束、清空落点、反向验证，并额外扫描其他几何，防止初始穿透遮住第二道墙。10Hz扫描与原预算、意图、站位、版本和位移屏障保持。

验证和交付：

- [x] 静态审查与`git diff --check`通过；对照UE5.7碰撞/导航接口，未新增测试框架或引擎代码修改。
- [x] 沿用本任务已确认的“编译并加载”许可，源码版`D:/UnrealEngine-5.7`、`GuLiStrikeEditor Win64 Development`构建退出0、37.64秒，8份BuildId一致。
- [x] 原型图已保存，回读12个相关实体；GiftEdge Note改为`(-17900,60000,-1777.22)`，Overview相机`(-23000,65000,6000)`。进入默认PIE，观察约第20秒正常赠品生成和路过单位；预期内部导航洞完整、重建受困成员继续原命令。
- [x] PID75840实际加载新DLL，一个编辑器、一个客户端；原生12029/55557连接恢复，临时诊断Blueprint/Actor已删除。
- [x] 同址PIE采样90.1秒、286份记录，59名现场成员全部存活并离开2500cm观察区；检测901次、查询284次、恢复87次、候选耗尽0次，最终无待恢复者。第40秒后该区没有无进展超过2秒的记录。两座相关建筑内部均不可导航。
- [ ] 玩家确认R2效果。导航异步重建期间曾有约5.6秒短暂停步；此次只覆盖用户报告的复现点，未扩大到大人口、停止/改令、完整障碍矩阵回归。

证据：[现场诊断及修正](../../Artifacts/MassNavRepair20260924/current-stuck/diagnosis.md)、[运行汇总](../../Artifacts/MassNavRepair20260924/current-stuck/verification-summary.json)、[构建](../../Artifacts/MassNavRepair20260924/compile-r2.log)、[BuildId](../../Artifacts/MassNavRepair20260924/build-ids-r2.json)、[模块加载](../../Artifacts/MassNavRepair20260924/loaded-build-r2.json)、[重开场景](../../Artifacts/MassNavRepair20260924/current-stuck/loaded-r2-scene.json)、[增量归档](../Archive/20260924-Mass同址卡死复现与R2修复.md)。当前PIE保留运行。
