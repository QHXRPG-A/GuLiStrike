---
schema: guli-progress/v1
id: DEV-20260913-001
work_id: WORK-20260913-001
kind: development
role: root
title: 单位受击白光与摧毁爆炸 — 技术方案
areas:
- combat
- vfx
- commander
- wingman
- ship
- resources
categories:
- art
- gameplay
status: done
verification: partial
created: '2026-09-13'
updated: '2026-09-14'
summary: 铁锈报废模型与僚机Chaos坠落已接入，继承原速度并触地销毁；保留已有爆炸、同步及对象池边界。
next_action: ''
relations:
  requirement: REQ-20260913-001
status_note: 09-14新增铁锈残骸与僚机Chaos坠落；源码Editor/Game构建和既有5项回归再次通过。Listen与远端已观察原速度继承、地形接触当帧销毁以及地面残骸清理。未新增测试，专服、完整伤害RPC、骨骼Actor死亡复活及GPU压力未覆盖。
---

# 单位受击白光与摧毁爆炸 — 技术方案

## 实现

### 铁锈报废模型与僚机坠落（2026-09-14）

- 新增[残骸Actor](../../Source/GuLiStrike/Gameplay/CombatEffects/GuLiUnitWreck.cpp)，复制可见StaticMesh或当前骨骼姿势，所有材质槽替换为`/Game/GuLiStrike/FX/UnitFeedback/M_UnitWreckRust`；尾焰、血条、描边、战斗组件不复制。原网格轮廓保留，未新增破碎几何资产。
- 僚机使用模型包围盒大小的Chaos碰撞体，开启重力、CCD和碰撞通知，线性阻尼为0，继承三维速度；初始局部翻滚角速度为`(18,9,0)°/s`。仅阻挡WorldStatic，支持表面法线Z>0.25的地面、坡面和屋顶接触后立即Destroy；无接触30秒兜底，保留引擎原有世界边界检查。
- 速度优先取Owner运行态，Remote取当前播放时刻的已接受速度样本。`ApplyRosterCut`在隐藏或替换成员代次前发出既有死亡表现通知，保留原Handle去重，复制残骸后正常Pawn继续走原对象池。旧Pawn已释放时，用轨迹姿态及相同模型构建残骸，不增加残骸网络复制或伤害提交。
- Actor死亡复制后隐藏原可见模型，复活恢复这些模型并删除该Actor的残骸；迟加入已死Actor不重播。Mass沿原5秒Wreck窗口按模型批次绘制锈蚀ISM，原存活ISM隐藏，保留脚环。批次变换不变时跳过更新。
- [材质构建脚本](../../Scripts/build_unit_wreck_material.py)：24节点、Opaque/DefaultLit，顶点局部坐标插值到像素供程序噪声，混合暗钢、铁锈与焦黑；发光为0，开启ISM/Skeletal用法。只保存项目新材质，原有20种地爆、2种空爆及商城模型不改。
- 独立Actor残骸最多64个，沿用180000cm裁剪，普通Actor5秒清理；Mass不创建逐兵Actor，沿既有最多500兵表现容量。Dedicated不创建反馈子系统。属于生命周期与预算设计，尚未实测GPU压力。
- 初轮两端观察中每次死亡产生1份残骸、重复通知仍为1，物理和重力开启且9个槽全为铁锈材质；Owner实测先前移动与初速度方向点积约0.999995。地面单位25号经既有权威扣血入口在Listen显示1个锈蚀实例，5.301秒已归零；该次远端相机裁剪下为0，不作为远端可见验收。初轮碰撞采样误用Python HitResult成员访问漏记事件，未作为落地证据；地图实际KillZ=-1048575，无需修改。
- 最终原生构建：源码`D:\UnrealEngine-5.7`，Editor Development exit 0（19.96秒）、Game Development exit 0（33.37秒）。引擎和项目BuildId均`26d441ba-b96a-4e7b-b104-c2c6cd3e663c`；本轮未改或重编原生插件。既有DamageLedger三项、HealthBar两项共5/5成功，无新增测试文件、用例或Gate。证据`Artifacts/UnitWreck/{editor-final-build.log,game-final-build.log,build-id.json,existing-tests/index.json}`。
- `wingman-final-runtime.json`在Listen侧Remote Pawn与远端Owner Pawn分别记录5.589/5.260秒Landscape接触与Destroy同帧；运动方向点积0.998188/0.999039，水平速度漂移0。最终材质v3再次观察，两端3.024/4.335秒触地当帧销毁，124/186帧采样水平速度漂移0，9个槽全为最终铁锈材质；时间随实际起点高度和初速度变化，不是定时删除。证据`Artifacts/UnitWreck/FinalLook/wingman-final-runtime.json`及`final-summary.json`。开始/坠落/后段/结束截图在同目录，手动曝光仅用于隔离模型审美，不改游戏相机配置。
- WM01单位37号生命300.5→0，既有`DamageMass`走真实权威扣血；临时把两端本地相机置于同一单位附近以避开距离裁剪，0.262秒、1.105秒两端各1个铁锈ISM，5.614秒均0；之后恢复视角。证据`Artifacts/UnitWreck/ground-final-runtime.json`及`ground_0/1_0/1/2.png`。该项在最终调色前验证生命周期，v3仅更改颜色参数。
- 最終材质24节点、无纹理依赖，v3重新部署saved=[]，0编译错误；PIE已停止，地图/内容脏包均0。重启前发现未保存的`BP_SC_Autocannon`，先备份磁盘版本至`Artifacts/UnitWreck/BeforeRestart/`再保存当时已有改动，未修改其图逻辑。
- 验证边界：僚机使用现有死亡表现入口，非整条实弹伤害RPC重放；本轮未重新跑Dedicated、GPU压力或打包，未实测骨骼Actor死亡/复活。既有SavedMove上限与CarrierSource序列化警告仍在，不据此宣称完整网络验收。详见[本次增量归档](../Archive/20260914-单位铁锈残骸与僚机物理坠落.md)。

- `UGuLiUnitFeedbackSubsystem`：仅渲染客户端创建。共享Additive白光材质；Actor受击时写CPD索引7为本地游戏时间，结束恢复旧Overlay。白色发光乘以`strength=saturate(1-(Time-HitStartTime)*2)`，0.5秒后为0；Opacity固定默认值1，本体材质和透明度不变。此项落实用户追加的“不要改变透明度”。
- `UGuLiUnitFeedbackComponent`：事件驱动，无Tick。既有Health组件在本地创建观察器，监听已确认血量；矿车默认组件监听引擎受伤与销毁。玩家飞船关闭NPC死亡爆炸。真实销毁在EndPlay(Destroyed)处理，避免Actor先结束组件再广播OnDestroyed导致遗漏。
- Commander：血量下降更新受击时间。仅当前闪白单位进入按模型分组的额外ISM批次，实例自定义数据索引0保存受击时间；跟随现有插值位置。Alive→Destroyed边沿播放死亡效果，初始死亡快照不播放。
- Wingman：既有公共GameState效果组件批量发送服务器确认的轻量受击通知；每50ms按单位合并，队列上限256，单RPC最多16项，过期1秒丢弃。可靠名册死亡边沿可补回丢失死亡表现；完整Wingman身份去重，不改变权威血量或位姿。
- 爆炸：A～T副本位于`/Game/GuLiStrike/FX/UnitFeedback`；保留原爆炸、烟、碎屑和0.75秒Shockwave，移除原动态灯光Emitter。`UGuLiCommanderDataSubsystem`模型目录中有效包围盒最长半边的最小值作为参考尺寸；`User.Scale = ReferenceExplosionScale × 单位模型尺寸 / 最小兵种模型尺寸`。艺术基准原为1.510524，后按用户“所有单位销毁爆炸同步放大5倍”改为7.552620，兵种间比例不变；删除半径500和僚机1500的常量，以及0.5～6的截断。Actor合并可见Static/SkeletalMesh的模型包围盒，保留组件与Actor缩放但去掉世界朝向，排除Niagara等附加特效；ISM按单个模型测量，不读取整批包围盒。缺模型采用参考观感，不猜测半径。
- 血条：沿用一个本地视口一个ISM批次，Commander血量下降的同一`HitFlashStartTime`驱动白光和血条；Actor的`FlashActor`记录同一时间，僚机既有服务器受击cue追加uint16千分比。完整显示3秒、0.5秒线性渐隐，连续命中刷新。选中单位常驻逻辑保留；未受击的残血单位不再永久显示。Actor槽位回收复用，死亡、隐藏、归池、切图清理。
- UI所有权：世界生命周期共享Renderer，HUD只持有引用；离开Commander视角不销毁其他角色正在使用的血条。`OnlyOwnerSee`对应的是SceneView的ViewActor，因此Renderer的Owner跟随本地Controller的ViewTarget，而非Controller自身；冷重启后的PIE截图已确认实际可见。UI只读权威血量和插值位置、不获取输入焦点；世界billboard由视口裁剪，沿用距离裁剪。灰色状态条标识没有正式HP的占位单位，不新增扣血规则。项目副本`M_UnitHitHealthBarWorld`采用Translucent/Unlit，ISM自定义数据2接Opacity，避免原Masked材质在渐隐中途突然裁掉。
- 生命周期：Niagara ManualRelease池化，单System池大小16且不预热；收到OnSystemFinished后归池，异常循环12秒强制释放。不能直接轮询IsComplete：Niagara等待初始化时尚无SystemInstance，也会返回true，已通过运行时定位并修正提前回收问题。并发上限64，距离180000cm裁剪；首次加载等待最多1秒。数值为首版预算配置，未当作实测性能收益。
- Config软引用及明确cook目录使生成资源和原包纹理/材质依赖进入打包。新代码不引入模块依赖。
- 僚机空爆追加：配置新增`WingmanExplosions`两项软引用，仍由同一个WorldSubsystem异步加载并拥有池化组件；`ApplyWingmanFeedback → QueueDestruction → SpawnExplosion`在客户端选择独立空爆池，原`PlayDestruction`蓝图API及其他单位A～T路径不变，不增加RPC。异步待播项携带类型，空爆池缺资源时不回退成地爆。Aerial资源不含`User.Scale`，因此使用组件变换接收同一模型比例计算结果；保留原始8个Emitter的启用状态，包括`smoke_shockwave`与`refr_mesh`，不删冲击波。自然结束/12秒兜底回池、并发上限、去重和专服抑制规则不变。

## 任务

- [x] 资源包盘点与编辑器能力探测。
- [x] 生成2个材质和20个Niagara副本，保存并读回冲击波与编译结果。
- [x] Actor、Commander、Wingman运行时接入及专用服务器抑制。
- [x] 配置随机候选、池化、裁剪和cook路径。
- [x] 构建`GuLiStrikeEditor Win64 Development`和`GuLiStrike Win64 Development`，比对源码版BuildId。
- [x] 运行时观察白光与爆炸起始、峰值、消散；检查复用与销毁清理。
- [x] 同链受击血条3秒保持、0.5秒渐隐和连续命中重新计时；冷重启后Dedicated双客户端读回并观察画面。
- [x] 读取最小兵种模型作为参考尺寸，真实WM01死亡在两客户端使用相对模型比例；清理窗口后活动数归零。
- [x] 更新战斗记录、增量归档及索引校验。
- [x] 僚机Aerial 3/4副本、独立随机池及冲击波保留；源码构建和现有死亡表现入口观察。

## 冲击波2.5倍与扑脸排查（2026-09-14最新）

- 通用A～T读取`Explosion001.InitializeParticle.Uniform Sprite Size`（A～C为1500，其余2000），读取Shockwave所用网格包围盒最大直径9.877644及增长曲线；原Shockwave直径约7408.233，约为主体4.939/3.704倍。仅将其`FloatFromCurve003.Scale Curve`从3改为1.518580/2.024774，最大直径约3750/5000，不改主火球、烟、碎屑或0.75秒时长。
- Aerial基准读取实际动态输入`main.RandomRangeFloat002.Minimum`与主火球尺寸曲线（峰值2），不用已被动态输入覆盖的Initialize默认值。按随机火球较小值1300/1200计算烟圈上限；保守包络为`2×(最大速度×最大寿命+最大烟粒子半径)`，不把Drag的减速作为放宽额度。烟圈速度区间500～1000分别收紧为220.833328～441.666656、200～400；所有Emitter仍保留。
- 用户进一步报告“所有爆炸冲击波扑脸”。确认材质Depth Test未被关闭；源码`NiagaraMeshParticleUtils.ush`先应用ParticleScale，Local Space分支又乘PrimitiveLocalToWorld。Aerial与Big_17的`refr_mesh`初始化已乘`Engine.Owner.Scale`，此前实际网格范围是倍率的平方，而非一次线性缩放。新增**仅编辑器、仅允许项目指定爆炸副本**的`NormalizeExplosionRefractionSpace`，将三个副本`refr_mesh`改为World Space；初始化仍取爆点位置，Owner.Scale仅应用一次。未改主体Billboard、全局5倍、伤害、复制或池化运行代码。
- [比例校准 helper](../../Scripts/unit_destruction_shockwave.py)、[批量校准入口](../../Scripts/calibrate_unit_destruction_shockwaves.py)、[空间修正入口](../../Scripts/normalize_explosion_refraction_space.py)可重复部署；既有两个构建脚本已接入相同比例规则。最终22项读回最大比值2.5、重复执行无新增缩减；3项空间修正再执行saved=[]。原商城资产未写入。
- 证据：`Artifacts/UnitFeedback/destruction-shockwave-2_5.json`记录初次校准值，`destruction-wave-final-readback.json`记录空间修正后的最终读回，`explosion-refraction-space.json`记录World Space与编译；23个受影响System均0错误/0警告。
- 源码版`D:\UnrealEngine-5.7`的Editor和Game Development均exit 0，耗时18.62/37.95秒；引擎和项目BuildId同为`26d441ba-b96a-4e7b-b104-c2c6cd3e663c`。无原生插件改动或重编；仅项目现有编辑器辅助库增加入口。日志`wave-camera-editor-build.log`、`wave-camera-game-build.log`。
- 原Listen+远端客户端配置：通过既有捕获命令在固定机位手动预览A、L、Aerial3、Aerial4、Big17冲击波，约0.06/0.15/0.65/2.5/5.5/6.5秒记录30个样本；6.5秒样本均inactive，两端UnitFeedback活动数为0。截图`TestResults/WingmanAttack/Wave25_{A,L,Aerial3,Aerial4,Big17}_{start,peak,wave,tail,late,end}.png`，报告`destruction-shockwave-2_5-temporal.json`。观察到局部爆点表现与消散，HUD/场景干扰仍在，**不当作像素级2.5倍或近镜头实战扑脸场景已完全复现的证据**。预览直接生成资产，不冒充伤害端到端或Big17原3秒运行时清理验收；原3秒清理代码未改。
- 维持原池大小16、64并发、180000cm裁剪与专服抑制；不新增粒子/透明层/动态灯/材质。收紧空间范围不宣称GPU优化实测；极端视角Bounds与打包未覆盖。最终停止PIE且脏包0。本轮无新增/修改测试用例。
- 增量归档：[冲击波外径上限与重复缩放修正](../Archive/20260914-冲击波外径上限与重复缩放修正.md)。

## 僚机空爆与统一5倍（09-13阶段）

- 两个项目副本：`NS_WingmanDestruction_Aerial_3`、`NS_WingmanDestruction_Aerial_4`；原件仍为用户指定AllExplosions/Aerial资源。每个保留原8个Emitter及其启用状态，`smoke_shockwave`、`refr_mesh`启用，原先禁用的`refr_sprite`不启用。当时脚本只配置共用EffectType、池大小16与不预热；09-14新增上述尺寸与空间修正。两者当时编译均0错误/0警告，重启后重复部署saved=[]。脚本：[build_wingman_destruction_assets.py](../../Scripts/build_wingman_destruction_assets.py)；历史读回`Artifacts/UnitFeedback/wingman-aerial-assets.json`。
- 其他单位A～T的20个`Shockwave`逐一读回为启用；证据`Artifacts/UnitFeedback/destruction-shockwave-inspection.json`。僚机保留空爆自带冲击波/折射层，不额外叠加旧地爆，也不修改对地轰炸的Big_17双层配置。
- 冷启动配置：`ReferenceExplosionScale=7.552620`，通用20候选、僚机2候选。Aerial不含`User.Scale`，组件Scale接同一模型比例结果；A～T继续使用原`User.Scale`通道。此处确认倍率配置与运行通道，不冒充固定机位像素级5倍对照。
- 源码版`D:\UnrealEngine-5.7`：Editor/Game Development均exit 0；引擎/项目BuildId均`26d441ba-b96a-4e7b-b104-c2c6cd3e663c`，本次无原生插件改动或编译。日志及门禁摘要`Artifacts/UnitFeedback/wingman-aerial-build-results.json`。既有HealthBar两项、DamageLedger三项再次5/5通过，报告`wingman-aerial-existing-tests/index.json`；未新增/修改测试。
- Listen server与远端客户端的运行池均出现Aerial 3/4，通用加载数仍为20；读回`Artifacts/UnitFeedback/wingman-aerial-live-pools.json`。继续通过既有`SetWingmanAlive`死亡表现入口观察：第一次活动数0→1，重复死亡通知仍为1；另两次独立通知分别选中3与4，重复后活动数分别保持1/2。这是表现入口观察，不是新建端到端伤害测试。
- Aerial 3：约0.088/0.262/0.757/2.007秒保持活动，Scale14.233981；5.003秒已归池，12.503秒仍释放。Aerial 4：约0.082/0.256/0.758/2.019秒活动，Scale14.441902；5.010秒已归池，12.507秒仍释放。读取实际模型的当前姿态/组件比例，不写死这些样本值。报告`wingman-aerial-3-temporal.json`、`wingman-aerial-4-temporal.json`；截图`TestResults/WingmanAttack/WingmanAerial3_{start,peak,wave,tail,end}.png`与`WingmanAerial4_{start,peak,wave,tail,end}.png`。确认火焰→烟雾→消失；远景与附近单位影响观察，未把冲击波启用等同于强度视觉验收，也未改变原冲击波亮度。
- PIE结束前两端活动数均0；结束后地图/内容脏包均0，仍打开`LVL_ShipWingmanAirCombatPrototype`，Listen+远端客户端设置不变。本轮未重跑Dedicated；自然运行日志有SavedMove上限与CarrierSource序列化警告，未修改网络代码，不据此宣称完整实战网络验收。GPU压力、极端视角Bounds与打包运行未覆盖。
- 最新增量：[僚机空爆随机二选一与销毁特效五倍](../Archive/20260913-僚机空爆随机二选一与销毁特效五倍.md)。

## 共链血条与模型缩放阶段验证（统一5倍调整前）

- 最终源码引擎固定`D:\UnrealEngine-5.7`。`GuLiStrikeEditor Win64 Development`和`GuLiStrike Win64 Development`均exit 0；引擎、项目BuildId均为`26d441ba-b96a-4e7b-b104-c2c6cd3e663c`。日志：`Artifacts/UnitFeedback/hit-bars-final-editor-build.log`、`hit-bars-final-game-build.log`。最终门禁后未再改原生代码。
- 既有`GuLiStrike.Commander.UI.HealthBar`两项与`GuLiStrike.Combat.DamageLedger`三项，最终5成功、0失败、0未运行；报告：`Artifacts/UnitFeedback/hitbar-final-existing-tests/index.json`。未创建、追加或修改测试用例。
- 同一WM01权威伤害在Dedicated两个客户端：0.203s、2.914s血条Opacity均为1；3.261s均约0.517；3.670s均为0且实例缩放归零。再次命中发生在约2秒时，首击后3.402s仍为1，5.256s进入渐隐，5.801s归零。时间以伤害发起为起点，含复制与帧边界延迟。读回：`Artifacts/UnitFeedback/hitbar-final-readback.json`和`hitbar-repeat-hit-readback.json`。
- 实际视口观察：`TestResults/WingmanAttack/HitBar13_final_0_2.png`、`HitBar13_final_3_25.png`、`HitBar13_final_3_65.png`；连续受击保持/消失见`HitBar13_repeat_3_4.png`、`HitBar13_repeat_5_8.png`。这些截图来自最终原生所有权修正后的新PIE，不依赖手动设置Renderer Owner。
- 当前最小模型最长半边为755.262024cm，WM01为2880.746338cm；该值是模型资产读回，不是代码尺寸常量。两客户端缓存相同参考值；最小兵种保留`User.Scale=1.510524`，WM01实际死亡随机选到B/M且均为5.761493，即最小兵种约3.814倍，12.507s样本活动数为0。证据：`Artifacts/UnitFeedback/model-size-death-readback.json`。
- 血条材质由[构建脚本](../../Scripts/build_unit_hit_health_bar_material.py)生成项目副本，6个表达式，编译0错误；原Commander材质不修改。收尾停止PIE，地图/内容脏包均为0，保留专服双客户端设置。
- 本次联合变更及验证边界见[增量归档](../Archive/20260913-单位受击血条与模型尺寸爆炸缩放.md)。

## 初版证据（历史）

- 资产生成入口：[build_unit_feedback_assets.py](../../Scripts/build_unit_feedback_assets.py)。
- 资产编译与图结构读回：`Artifacts/UnitFeedback/asset_build.json`。
- 源码：[共享表现](../../Source/GuLiStrike/Gameplay/CombatEffects/GuLiUnitFeedbackSubsystem.cpp)、[Actor事件组件](../../Source/GuLiStrike/Gameplay/CombatEffects/GuLiUnitFeedbackComponent.cpp)。
- 源码版`D:\UnrealEngine-5.7`：Editor、Game Development均退出0；最终两份模块BuildId同为`26d441ba-b96a-4e7b-b104-c2c6cd3e663c`。记录：`Artifacts/UnitFeedback/build-results.json`、`build-id.json`及对应构建日志。
- 当前已有`GuLiStrike.Combat.DamageLedger`自动化3/3通过；见`Artifacts/UnitFeedback/editor-runtime.log`。没有增加或修改测试用例。
- Commander：既有`GuLiTeleportQALibrary.damage_mass`走权威伤害入口；白光材质时间后0.026s/0.251s存在1个活动实例，0.521s归零。本体材质未变，Additive只衰减Emissive。证据：`commander-hit-runtime.json`和`hit_start.png`、`hit_mid.png`、`hit_end.png`。
- Commander死亡：随机选中L，火球和向外折射冲击波可见；约5.015s自然结束归池，12.1s活动数仍为0。矿车标准AnyDamage回调成功闪白，0.520s恢复Overlay；实际Destroy随机选中A并完成回收。证据：`commander-death-runtime.json`、`miner-hit-runtime.json`、`miner-death-runtime.json`及`explosion_peak.png`、`explosion_wave.png`。
- Listen PIE及晚加入Air角色中，现有飞船HealthChanged委托能触发白光且结束清理，NPC爆炸开关为false；现有远端僚机调用表现受击入口后闪白，SetWingmanAlive死亡边沿使活动爆炸数0→1，重复通知仍为1，回池立即清除Overlay，结束活动数0。证据：`air-feedback-runtime.json`。该项是现有Actor表现入口观察，不冒充完整伤害RPC端到端重放。
- 初次专用服务器PIE中，服务器没有反馈子系统，两客户端各成功加载20个候选。最终已停止PIE，恢复原有专服双客户端设置；地图和内容脏包均为0。
- 增量归档：[20260913-单位受击白光与摧毁爆炸](../Archive/20260913-单位受击白光与摧毁爆炸.md)。

## 验证边界

构建引擎固定`D:\UnrealEngine-5.7`。现有素材原版包含GPU发射器、透明烟雾和折射冲击波，因此大量同屏爆炸的实际GPU开销仍需目标硬件采样；池化只减少创建回收成本。完整实战丢包重放、打包运行未覆盖，所以verification保留partial。未新增自动化测试或扩展相邻模块验收。
