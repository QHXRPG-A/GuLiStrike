---
schema: guli-progress/v1
id: DEV-20260820-001
work_id: WORK-20260820-001
kind: development
role: root
title: DIY 飞船（模块化装配 + 飞行中热切换） — 技术方案
areas:
- ship
- combat
- assets
- learning
categories:
- art
- gameplay
status: in_progress
verification: partial
created: '2026-08-20'
updated: '2026-09-05'
summary: 资产：Content/Assets/Arma/CombatAvatarFly/bsgvirgonexpresscarina/StaticMeshes/bsgvirgonexpresscarina
next_action: 用户手动加 7 个 socket（教程见下，坐标已定稿）
relations:
  requirement: REQ-20260820-001
status_note: 实施中
---

# DIY 飞船（模块化装配 + 飞行中热切换） — 技术方案

- 对应需求：[DIY 飞船](../RequirementDocument/20260820-DIY飞船.md)

## 技术选型

| 决策点 | 结论 | 为什么不用 X |
|---|---|---|
| 主体类型 | `AGuLiStrikeShip : ACharacter` | 用户定稿；Character 自带胶囊碰撞 + CharacterMovement(Flying) 即可做 6DOF 街机飞行。不用 Pawn+FloatingPawnMovement（用户要求 Character）；不继承现有 `AGuLiStrikeCharacter`（那套绑定俯视相机与平面约束，twin-stick 专用） |
| 部件表示 | `UGuLiStrikeShipPartComponent : UStaticMeshComponent`，具体部件 = 蓝图组件子类 | 用户定稿"部件=组件"；直接继承网格组件免去双层包装；蓝图子类填网格/材质/数值/兼容槽位，加部件零代码。不用 DataAsset+动态生成网格（用户架构为先组件类） |
| 槽位实现 | 舰体 StaticMesh 上加 socket（**用户手动加**，见下方教程） | Python 的 `StaticMesh.sockets` 属性 protected 读写被拦（已实测）、`EditorStaticMeshLibrary`/`StaticMeshEditorSubsystem` 无 socket API（已实测）；C++ 工具函数方案被用户改为手动添加，量少（7 个）可接受 |
| 兼容性 | 部件上 `TArray<FName> CompatibleSockets` | 用户定稿"接入名集合"；一个部件可接多个槽位（火箭巢示例） |
| 6DOF 移动 | CharacterMovement `MOVE_Flying` + 关平面约束；WASD 局部平移、鼠标 pitch/yaw、Q/E 滚转（`AddActorLocalRotation`） | 不用物理仿真（FloatingPawn/ChaosVehicle 引入复杂度且用户选了 Character 路线） |
| 武器实现 | 遍历已装武器部件，按各自射速从部件世界变换生成现有 `AGuLiStrikeProjectile` | 复用现有投射物；GAS 虽启用但项目零使用（Build.cs 无依赖），不为此引入 |
| 输入 | EnhancedInput：`IMC_Ship` + IA_Ship_*（Thrust/Look/Roll/Fire/Boost/CycleEngine/CycleWeapon） | 项目全EnhancedInput；Python 建映射若有 API 缺口退 C++ `BindKey`（项目有先例 GuLiStrikeCharacter.cpp:117） |

## 已验证的关键事实

- virgon 网格：14731×4973×5895cm（长×宽×高），148,700 三角面，单 LOD，无 Nanite，碰撞已有 1 个凸包（够用）
- virgon 的 9 个材质槽**槽名即原始材质名**（Credits_EVE-Kaneda_NepsterCZ / Flattop_bridge / Flattop_bridge_window / Flattop_engineering_pod / Flattop_hull_bay / Flattop_hull_structure / Flattop_machinery_bottom / Flattop_machinery_top / Flattop_signal_lights），按名对号入座已全部赋值并保存（2026-08-20）
- fly-01 散件：cockpit / engines / intakes / lasers / rocket（另有 space_mouse_fbx 整机、stripes 条纹）
- Python `set_editor_property('static_materials', 新数组)` 可整体替换材质槽（逐元素改不生效，须新建数组回写）

## 涉及模块

| 变更点 | 路径/类 | 类型 |
|---|---|---|
| 飞船主体 | `Source/GuLiStrike/Gameplay/Ship/GuLiStrikeShip.h/.cpp` | C++ |
| 部件基类（含 FGuLiStrikeShipStats） | `Source/GuLiStrike/Gameplay/Ship/GuLiStrikeShipPartComponent.h/.cpp` | C++ |
| 引擎部件 | `Source/GuLiStrike/Gameplay/Ship/GuLiStrikeEnginePart.h/.cpp` | C++ |
| 武器部件 | `Source/GuLiStrike/Gameplay/Ship/GuLiStrikeWeaponPart.h/.cpp` | C++ |
| 飞船蓝图 | `Content/GuLiStrike/Blueprints/BP_GuLiStrikeShip` | 蓝图 |
| 载具模式 | `Content/GuLiStrike/Blueprints/BP_ShipGameMode` | 蓝图 |
| 部件蓝图 ×4 | `Content/GuLiStrike/Ship/BP_Engine_Standard / BP_Engine_Heavy / BP_Weapon_Laser / BP_Weapon_RocketPod` | 蓝图组件 |
| 输入 | `Content/GuLiStrike/Input/IMC_Ship` + `Actions/IA_Ship_*` ×7 | 输入资产 |
| 测试图 | `/Game/Maps/LVL_ShipTest` | 关卡 |
| 网格体改造 | virgon 加 socket（用户手动）、材质已修 | 资产 |

## 核心接口设计（C++）

```
UGuLiStrikeShipPartComponent : UStaticMeshComponent
  ├─ CompatibleSockets: TArray<FName>          // 可接入槽位名集合
  ├─ PartRelativeTransform: FTransform          // 相对 socket 的微调（位置/朝向/缩放）
  ├─ PartMass: float
  ├─ CanAttachToSocket(FName) → bool
UGuLiStrikeEnginePart : UGuLiStrikeShipPartComponent
  └─ Thrust: float
UGuLiStrikeWeaponPart : UGuLiStrikeShipPartComponent
  └─ Damage / FireRate / ProjectileClass / MuzzleOffset

AGuLiStrikeShip : ACharacter
  ├─ HullMesh(StaticMeshComponent, virgon) + SpringArm(追尾) + Camera
  ├─ DefaultParts: TArray<{SocketName, PartClass}>     // 出生自带配置
  ├─ PartCatalogue: TArray<PartClass>                   // 全量部件类，热切换在其中循环
  ├─ InstallPart(Class, SocketName) → bool              // 兼容校验→销毁旧→NewObject→挂 socket→注册→重算
  ├─ UninstallPart(SocketName) / GetPartAt(SocketName)
  ├─ CycleEngineParts() / CycleWeaponParts()            // 热切换按键入口
  ├─ RecomputeStats()                                   // Σ推力/Σ质量→推重比→Movement 参数
  └─ Fire()                                             // 遍历武器部件按射速出弹
```

## 数值草案（蓝图可调）

| 数值 | 值 |
|---|---|
| HullMass / BaseMaxSpeed / BaseAccel | 100 / 2000 / 800 |
| 引擎A 标准引擎（engines 网格） | 推力 600 · 质量 10 |
| 引擎B 重型引擎（engines 网格放大） | 推力 1000 · 质量 30 |
| 武器A 激光炮（lasers 网格） | 伤害 10 · 3 发/s |
| 武器B 火箭巢（rocket 网格） | 伤害 40 · 0.8 发/s · 兼容 HP_Weapon_L/R + HP_Engine_L（多槽位示例） |
| 推重比 | Σ推力 ÷ (HullMass + Σ部件质量) |
| MaxSpeed | Base × clamp(推重比 ÷ 标称比 6.0, 0.5, 2.0) |

## 任务清单

- [x] virgon 9 材质槽按槽名赋值 + 保存（2026-08-20）
- [x] virgon 碰撞确认（已有 1 凸包）
- [x] fly-01 部件材质槽检查（**发现：fly-01 的 cockpit/engines/lasers/rocket 等实为 MaterialInstanceConstant，不是网格**；唯一网格 space_mouse_fbx 是整机组装件。部件网格改用 ParagonProps：SternPod1d/SternPod1c/SM_Turret/Dawn_Gate_Orb，材质齐全）
- [x] 截图+几何质心交叉验证舰体朝向：**+X=艏部（信号灯 X≈+3978）/ -X=艉部（轮机舱 X≈-3464、mach_top X≈-4800）**，socket 表无需取反
- [ ] 用户手动加 7 个 socket（教程见下，坐标已定稿）
- [x] C++：GuLiStrikeShipParts.h/.cpp（部件基类 + 引擎/武器子类）
- [x] C++：GuLiStrikeShip.h/.cpp（装配 API/6DOF/数值聚合/开火）
- [x] 编译通过（Live Coding 对新类反射不完整 → 关编辑器全量编译 + 重启后正常；期间修复 Tripo3DUEBridge 缺失的 ThirdParty/IXWebSocket）
- [x] 蓝图：BP_GuLiStrikeShip（virgon 舰体/胶囊 800×2000/缩放 0.3/13 输入指针/DefaultParts×4/Catalogue×4）
- [x] 蓝图：4 个部件蓝图（网格/数值/兼容槽位全部程序化验证）
- [x] 蓝图：BP_ShipProjectile（Sphere 网格 0.4 缩放，不依赖 LFS stub 的旧投射物 BP）
- [x] 输入：IMC_Ship + IA_Ship_* ×13（WASD/QE/Space/C/Mouse2D/LMB/LShift/R/T；Python DataAssetFactory 创建）
- [x] BP_PC_Ship（清空 DefaultMappingContexts，避免与旧 IMC 冲突）+ BP_ShipGameMode（AGameModeBase 直系）
- [x] LVL_ShipTest 测试图（GameMode 覆盖/PlayerStart/太阳+天光+大气/5 个参照方块）
- [x] PIE 验证（无 socket 前可验证项）：飞船生成 ✓ MOVE_FLYING ✓ 缩放 0.3 ✓ 数值链路 ✓（0 引擎→推重比 0→极速钳到下限 1000）；InstallPart 对不存在 socket 返回 False ✓；CycleParts/FireInstalledWeapons 调用不崩溃 ✓
- [x] PIE 验证（socket 加入后，2026-08-20 下午完成）：**4 部件自动装配**（SternPod1d×2 + SternPod1c + SM_Turret）、推力 2200/质量 160 → 极速 4000（满档 ×2.0）、MOVE_FLYING ✓
- [x] 改进 1（2026-08-20 晚）：**鼠标只控制摄像机环绕，不改变飞船朝向**；新增推进时自动转向移动方向（bOrientToMovement/OrientTurnSpeed，全蓝图可调）、相机臂俯仰钳制 ±80°。全量编译+重启后 BP 配装数据完好（再次验证 Live Coding 才会清数据）
- [x] 改进 2（2026-08-20 深夜）：**飞船朝向不再影响摄像机朝向**——相机臂开 AbsoluteRotation（世界空间观察角）。实现走 BP 模板（C++ 同步写入构造函数，待下次编译生效）。PIE 动态验证：旋转飞船后相机世界朝向变化 <0.5°。**坑位记录：程序化设 bInheritPitch/Yaw/Roll=false 不会自动转绝对旋转（编辑器 UI 才会做该转换），必须显式 bAbsoluteRotation=true**
- [x] 改进 4（2026-08-21）：**巨型舰机动手感 + 修复按 S 原地打转**。① 参数全面调重：BaseMaxSpeed 2000→1200、BaseAcceleration 800→400、RollRate 90→30、OrientTurnSpeed 3.0→1.0、BrakingDecelerationFlying 200→60（松手保留惯性）；② 打转根因：自动转向追踪**速度方向**，S 产生反向速度→船头追 180°→推力方向随转→正反馈画圈。修复：改为追踪**推进意图**，且意图与船头余弦 > OrientMinForwardDot(0.3) 才转向——S 变成反推减速、纯侧移不转船头。PIE 验证：极速 2400/加速 800/刹车 60/滚转 30°/s 全部生效，配装 4/4 无损
- [x] 改进 5（2026-08-21）：**控制方案重构（用户定稿）**。① W+D/W+A 转向不灵 → OrientTurnSpeed 1.0→2.5；② Q/E 从滚转改为**偏航转向**（YawRate=40°/s），转向时机身向转弯侧压弯倾斜 MaxBankAngle=5°（FInterpTo 平滑、松手回正、BankInterpSpeed=4）。C++ 属性 RollLeft/RightAction 更名 TurnLeft/RightAction（BP 已重绑 IA_Ship_RollLeft/Right，IA 资产名未改）。压弯通过 Tick 直接接管滚转分量实现（与自动转向的零滚转目标不冲突）。PIE 验证：部件 4/4、全部新参数生效
- [x] 改进 8（2026-08-21）：**行为下沉到部件类（多态分发，用户架构定稿）**。原实现是 Ship 轮询 + Cast 类型判断（飞船类是大脑、部件是数据袋）；重构为：部件基类新增 `ContributeStats(FGuLiStrikeShipStats&)`（基类贡献质量，引擎重写叠加推力）与 `Fire(AActor*)`（基类空实现，武器重写自查冷却+从自身炮口出弹）；Ship 的 RecomputeStats/FireInstalledWeapons 变为无差别多态遍历，**不再出现任何 Cast 部件类型的代码**。新增部件类型（护盾/发电机等）零飞船代码改动。PIE 端到端验证：数值与重构前完全一致（推力 2200/质量 160/极速 2400）、武器部件自行出弹 1 发、热切换推力 2200→2600 正确
- [x] 改进 11（2026-08-21）：**Q/E 偏航惯性模型**。原实现按键每帧直接转、松开急停；改为角速度状态机：按住时 `YawVelocity` 平滑爬升至 ±YawRate（YawResponseSpeed=3），松开按阻尼衰减（YawStopDamping=1，角速度从 40°/s 衰减约 2-3 秒到停，<0.05°/s 归零）——巨型舰转向带角动量。旋转应用仍在 Tick 统一做（欧拉 yaw 分量，保俯仰）；压弯改为跟随**实际角速度**（惯性旋转期间保持倾斜）。PIE 冒烟：新参数注册（yaw_rate=40/response=3/damping=1）、装配/数值无回归
- [x] 改进 10（2026-08-21）：**三项架构加固**。① ContributeStats/Fire 改 `BlueprintNativeEvent`——蓝图部件子类可直接重写实现新行为（C++ 默认实现保留）；坑：UHT 不支持 const 限定，实现签名须无 const。② 部件基类重写 `OnComponentDestroyed` → 通知 Ship `NotifyPartDestroyed` 同步注册表；UninstallPart 改为"先注销再销毁"避免双重同步（外部直接销毁部件的兜底由此打通——部件击毁玩法的基础）。③ 新增 `FGuLiStrikeStatModifier` 叠加层 + `AddStatModifier/RemoveStatModifier`（同名覆盖/按名移除）：所有倍率连乘，**移动参数只在 RecomputeStats 一处写入**（单一仲裁点，超载/减速等 buff 不再直写参数）。PIE 动态验证：修饰 2400→2880→1440→1200→2400 全命中；外部销毁引擎后 PARTS 4→3、推力 2200→1600、极速 2133.33 自动重算（与手算一致）；开火正常
- [x] 改进 9（2026-08-21）：**文件拆分（用户定稿结构）**：`GuLiStrikeShipParts.h/.cpp`（基类+引擎+武器三合一）拆为三组文件——`GuLiStrikeShipPartComponent`（基类+统计聚合结构）/ `GuLiStrikeEnginePart` / `GuLiStrikeWeaponPart`，一组件一文件。类名与 `/Script/GuLiStrike.*` 路径不变 → 蓝图引用零影响。PIE 验证：类路径不变、部件 4/4、数值一致（2200/160/2400）、武器出弹正常
- [x] 改进 7（2026-08-21）：**修复 Q/E 漂俯仰角**。根因：偏航用 `AddActorLocalRotation` 绕飞船局部上轴旋转，而压弯让局部上轴倾斜 5°——绕歪轴偏航 = 船头画圆锥，俯仰随转向相位漂移（松键时机不同残差不同，故"有概率"）。修复：偏航改为欧拉分量级修改（只动 Yaw 分量，等效绕世界竖直轴转机头，Pitch 严格不动）。PIE 动态验证：pitch=45° 下偏航 +30° 后 pitch 保持 45.00 不变。**教训：任何"只改一个轴"的旋转需求，优先改欧拉分量而不是局部四元数旋转，尤其姿态里还叠加了其它轴（压弯）时**
- [x] 改进 6（2026-08-21）：**控制定稿：W+侧移只平移不转向**（bOrientToMovement 默认改 false，自动转向逻辑保留可开）；侧移输入（A/D，含 W 组合）驱动压弯——机身向侧移方向倾斜 5°、输入结束平滑归位（与 Q/E 偏航压弯共用滚转接管：BankDirection = clamp(转向输入+侧移输入)）。PIE 验证：orient=False、配装 4/4、转向键位绑定无损
- [x] ✅ 已解决（2026-08-21）：**UBT 编译静默崩溃根因 = 中文文件名**。git 默认 `core.quotepath=true` 把 `Progress/Gameplay/飞船.md` 输出成带引号的八进制转义串，UBT 的 GitSourceFileWorkingSet 把引号一起当路径喂给 Path.Combine → ArgumentException 硬崩（日志缓冲丢失导致看似死在 git-status 行）。修复：`git config core.quotepath false`。教训：**UE 项目里放非 ASCII 文件名时，先设 quotepath=false**
- [x] 改进 3（2026-08-20 深夜）：**修复镜头歪斜**——上一步设相机初始角时 `unreal.Rotator(-18,0,0)` 实际写入的是滚转 -18°（该构造器参数顺序与直觉相反，本会话两次踩坑），改为逐字段 `set_editor_property('pitch'/'yaw'/'roll')` 设为 (-18, 0, 0)。PIE 验证：出生与转船后相机 roll 恒为 0（水平 + 独立双通过）。**教训：Python 改 Rotator 一律逐字段设值，禁用位置参数构造**
- [x] 归档 + Gameplay/飞船.md + README 索引

## 调试记录（2026-08-20 下午：用户加 socket 后"飞船动不了"）

两个叠加根因，均已修复：

1. **IMC 键位映射写错属性**（根因 1，输入全无响应）：5.7 运行时读 `DefaultKeyMappings.mappings`（新结构体属性），创建时写入的是弃用的 `mappings` 数组 → 运行时不认。修复：用 `IMC.map_key(action, key)` API 逐条写入 13 个映射到新属性（回读验证 13/13），清空旧属性，保存资产。**教训：5.7 用 Python 建映射必须走 map_key()，不要碰弃用的 mappings 属性。**
2. **Live Coding 清空了 BP 的 USTRUCT 数组数据**（根因 2，部件不装配）：注释汉化后 Live Coding 重注册类，`DefaultParts`（结构体数组）里的 socket 名与部件类引用全部被置 None（object 指针数组如 PartCatalogue/输入指针幸存）；且运行中的部件类变成 `LIVECODING_GuLiStrikeShipPartComponent_0` 与蓝图继承的原生类不同型，无法回写。修复：关编辑器 → 全量编译 → 重启 → 重写 DefaultParts（4 条验证通过）。**教训：改了 Ship 相关源码后禁止依赖 Live Coding 保数据，必须全量编译+重启并检查 BP CDO。**
3. 附带发现：编辑器重启后恢复的关卡可能是 LVL_Main（无 GameMode 覆盖 → 生成 DefaultPawn）。**必须从 LVL_ShipTest 打开再 Play。**

## 手动加 Socket 教程（用户操作）

资产：`Content/Assets/Arma/CombatAvatarFly/bsg_virgon_express_carina/StaticMeshes/bsg_virgon_express_carina`

1. 内容浏览器双击该网格体 → 打开静态网格编辑器
2. 菜单 **Window → Socket Manager** 打开 socket 面板
3. 面板点 **Create Socket**（+ 号），把新建 socket 重命名为下表名字
4. 保持 socket 选中，在 **Details** 面板里直接抄下表的 **Relative Location / Relative Rotation**（单位 cm / 度）
5. 7 个加完后 **Ctrl+S** 保存资产

Socket 坐标表（✅ 已按几何质心验证的朝向定稿：+X=艏、-X=艉，直接抄数值即可）：

| Socket 名 | Relative Location | Relative Rotation | 用途 |
|---|---|---|---|
| HP_Engine_L | (-6500, -1100, 0) | (0, 180, 0) | 左引擎（喷口朝 -X 艉部） |
| HP_Engine_R | (-6500, 1100, 0) | (0, 180, 0) | 右引擎 |
| HP_Engine_T | (-6300, 0, 1800) | (0, 180, 0) | 顶部引擎 |
| HP_Weapon_Nose | (6900, 0, -300) | (0, 0, 0) | 艏部武器 |
| HP_Weapon_L | (2500, -2600, 300) | (0, 0, 0) | 左舷武器 |
| HP_Weapon_R | (2500, 2600, 300) | (0, 0, 0) | 右舷武器 |
| HP_Utility_Belly | (0, 0, -3700) | (0, 0, 180) | 腹部辅助槽 |

说明：socket 的 +X 朝向 = 部件的"前方"。引擎槽转 180° 使部件喷口朝艉部；武器槽不转（部件朝艏）。部件蓝图里还有 `PartRelativeTransform` 可再微调。

## 风险与备忘

- **遗留（用户操作）**：virgon 尚未加 socket → 部件装配/热切换/开火的完整链路待 socket 加入后生效（无 socket 时 InstallPart 安全拒绝并打 Warning）
- **遗留（既有问题，非本次引入）**：`Content/GuLiStrike` 下 19 个 .uasset 是 Git LFS 指针 stub（含 BP_GuLiStrikeCharacter/GameMode/PlayerController/Projectile 与全部旧输入资产），本地 twin-stick 主玩法因此不可运行；`git lfs pull` 可恢复。飞船 MVP 全部新资产，零依赖这些 stub
- **修复（顺手）**：`Plugins/Tripo3DUEBridge-UE5.7-Win64` 缺 ThirdParty/IXWebSocket，已从 `D:/UE5.7/test1/Tripo3d_UE_Bridge/Tripo3d_UE_Bridge/ThirdParty` 复制补齐，UBT 恢复可用
- IMC 的 `mappings` 属性在 5.7 标记弃用（推荐 DefaultKeyMappings）；当前写入有效，若 PIE 键位不响应需迁移
- 键位手感（MousePitch/YawScale、RollRate、BrakingDecelerationFlying=200）未实测调参，全蓝图可调
- virgon 148K 三角面单 LOD：MVP 桌面可接受，后续可加 LOD 或 Nanite
- 武器 Damage 已作为部件数值聚合（GetTotalThrust 等可查），但现有 NPC 为一击必毁无 HP 系统，伤害实际生效需后续接伤害管线
- Live Coding 教训（已记入归档）：新增 UCLASS 后 Live Coding 编译虽成功，但蓝图系统拿不到新类 UPROPERTY 反射（set_editor_property 报 property 不存在），须关编辑器全量编译重启

## 结果链接

[归档记录（0820-0821 总归档，整合 6 份分项归档）](../Archive/20260821-DIY飞船开发总归档-0820至0821.md)
