# Mass 分帧寻路源码静态审查 — 2026-09-23

基线是本轮开始时的工作区，副本见`baseline/`及`baseline.json`；不是git HEAD。没有编译、运行游戏、新增或运行自动化测试。

- 22个变更C++头/源文件经UE宏归一语法解析无错误；证据`static-grammar.json`保留各文件SHA256。此检查不替代UHT、完整类型检查与链接。
- `git diff --check`相关Source目录退出0。Build.cs为运行模块引入RenderCore/RHI，未改引擎源码。
- 手动/自动/导航代次修复/个人恢复使用同一世界帧预算。lane/slot、编队宽度及运动过程旧多边形引用刷新也计入；不再保留未使用的同步路径 helper。出生位置预检与普通10Hz位移积分不是命令终点规划，不改变其既有规则。
- 候选按行、匹配按增广步骤、投影按成员/候选、路径按任务、连接按单条、准备/提交按25人工作单元推进。时间检查发生于工作单元边界，查询超出进入时剩余预算计数。64/4次数与100成员为默认硬计数，原子工作单元不可抢占。
- 预约账本按成员ID与空间格索引，提交前用实时账本复核；失败成员恢复当前旧预约。换局清空预约/终点/转向验证缓存；未提交进度按任务版本失效，已经发出的Committed不会再次发送。
- 相同命令批次共享BatchOrderId，实际起点在提交时取值；导航代次失效重新进入规划，已提交组不回滚。新命令通过任务版本阻止旧结果覆盖；个人恢复同样核对任务/订单/导航/战局版本。
- 网络源使用终点和名册事件；生产路径没有每连接全终点快照调用。状态与终点一致后才排队；基线身份复制一次，实体状态按游标构建。v19编码字段/顺序保持，ACK只依赖完整批解码与应用。
- 客户端绿线取消选择/移除/死亡/订单过期先隐藏，队列消费时再次检查最新选择、状态及终点；相同订单更新保持已显示线。SceneProxy捕获不可变顶点数据，渲染命令队列顺序保护proxy销毁；缓冲在渲染线程释放，InitFromDynamicVertex不重复初始化其已初始化的资源。
- 注册表快照半径覆盖格、独立环境候选名额；射线局部连通防止绕墙后误判直达；贴障绕行方向带滞回，恢复同样受环境约束；速度只在成功位移后用实际位移/实际步长回写。

对照本机UE5.7源码接口完成渲染资源与NavMesh Raycast调用审查。没有以静态审查证明编译或运行效果。场景保存与配置证据另见`scene-delivery.json`及`scene-nav-settings.json`。
